services:
  mock-api:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      MOCK_MODELS: "true"
      CORS_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"
      DATA_DIR: /data
    volumes:
      - mock_api_data:/data

volumes:
  mock_api_data:
