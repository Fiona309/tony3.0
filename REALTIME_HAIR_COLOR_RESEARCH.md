# 实时染发效果方案调研

> **日期**：2026-08-04
> **背景**：对标施华蔻「染发魔镜」小程序 与 美图/抖音染发特效，评估用实时渲染替代当前 AI 生图方案
> **结论先说**：技术完全可行，**关键资产你仓库里已经有了**，成本从 ¥0.28/次 降到 **¥0**，延迟从 10 秒降到 **每帧 50-70ms**

---

## 0. 一句话结论

施华蔻用的**不是生图模型**，是「实时头发分割 + 亮度保留式重染色」。这套技术 Google 已开源，模型文件（`hair_segmenter.tflite`）**此刻就躺在你仓库的 `hair_full_pipeline/models/weights/` 里**；而让它显得「专业」的那份底色度数映射表，也已经在 `hair_full_pipeline/data/` 里了。

---

## 1. 施华蔻染发魔镜的原理

### 1.1 核心技术栈

不是 AI 生图。是这三步，每帧都跑：

```
摄像头帧 → ① 头发分割(神经网络) → ② 亮度保留式换色 → ③ 边缘羽化合成 → 输出
```

**① 头发分割**：一个专门训练的小型 CNN，输出每个像素「是不是头发」的概率图。Google 的论文 *Real-time Hair Segmentation and Recoloring on Mobile GPUs*（arXiv 1907.06740）就是这套方案的公开版本，论文明确说「已部署在主流 AR 应用中，服务数百万用户」。

**② 亮度保留式换色**（这是「逼真」的关键）：
转到 Lab 色彩空间后，**只替换 a/b 两个色度通道，保留原始的 L 亮度通道**。因为发丝的纹理、高光、阴影、层次感**全部编码在亮度里**——保住 L，头发就还是那束头发，只是换了颜色。这也是为什么它看起来不像"涂色块"。

**③ 时序一致性**：把上一帧的 mask 作为额外输入通道喂给网络，避免逐帧抖动。

### 1.2 「布丁头」为什么能区分深浅（你特别问的这点）

**这不需要额外做什么，是亮度保留方案的自然结果。**

- 发根未漂过 → 原始亮度 L 很低（暗）→ 换色后仍然暗 → 呈现「染不上色」
- 发尾漂过 → 原始亮度 L 高（亮）→ 换色后色彩鲜艳饱满

我在验证 demo 里加了一行 `receptivity = clip((L - 25) / 55, 0, 1)` 来强化这个物理规律 —— 亮度越高，上色越强。这一行就复现了布丁头效果。

### 1.3 那个「9度 / 漂浅1次 / 漂浅2次」滑块

这才是施华蔻比美图**专业**的地方，也是真正的壁垒 —— **不是算法，是染发行业数据**：

同一支染膏，涂在不同底色度数的头发上，出来的颜色完全不同。所以它需要一张「色系 × 底色度数 → 实际呈色」的查表。

**你仓库里已经有这张表了**：

```
hair_full_pipeline/data/hairdye_palette_mapping.json
  → 13 个色系 × 底色度数 5~9 = 65 条映射
  → 每条含真实的 rgb / lab / hex
  → 来源标注：「黄皮显白发色推荐 底色度数表（权威版）」

hair_full_pipeline/data/nutralization_rules.json
  → 色彩中和规则（红↔绿、黄↔紫…）
```

滑块选度数 → 查表拿到该度数下这个色系的真实呈色 RGB → 送进第②步。**逻辑完全打通。**

### 1.4 施华蔻用的是谁家的 SDK

没有公开资料能确认。国内做这块的主要是：
- **玩美移动（Perfect Corp / YouCam）** —— 已把虚拟试妆服务扩展到美发和抖音小程序，可能性最大
- **ModiFace**（欧莱雅收购）—— 但施华蔻属汉高，是欧莱雅竞对，用的可能性低
- 帕克西、弥知科技等国内 AR 试妆服务商

---

## 2. 美图 / 抖音染发特效的原理

**技术路线完全一样**，只是分割模型更小更快、精度更低——所以你观察到的「分区没施华蔻准确、不够逼真」是准确的判断。

差距具体在：
| 维度 | 施华蔻 | 美图/抖音 |
|---|---|---|
| 分割精度 | 高（专门训练，发丝级） | 中（通用模型，边缘粗糙） |
| 底色度数建模 | 有（专业查表） | 无（统一贴色） |
| 目标 | 卖染膏，必须准 | 娱乐效果，好看优先 |

作为降级方案完全成立。

---

## 3. 开源项目

### 3.1 首选：MediaPipe Image Segmenter（Google，Apache 2.0）

**这就是你仓库里那个 `hair_segmenter.tflite`。**

官方 benchmark（Pixel 6）：

| 模型 | 输入尺寸 | CPU | GPU |
|---|---|---|---|
| **HairSegmenter** | 512×512 | 57.90 ms | 52.14 ms |
| SelfieMulticlass（含头发类） | 256×256 | 217.76 ms | 71.24 ms |

- **平台全覆盖**：Web/JavaScript、Android、iOS、Python 都有官方 API
- 模型仅 763 KB
- Web 端完全在浏览器本地跑，摄像头数据不出设备（隐私合规友好）

### 3.2 其他可参考实现

| 项目 | 说明 |
|---|---|
| [dkurt/hair_segmentation](https://github.com/dkurt/hair_segmentation) | 用 OpenCV 跑 MediaPipe 分割模型，最简参考 |
| [razvan404/hair-color-changer](https://github.com/razvan404/hair-color-changer) | U-Net 分割 + 摄像头换色 demo |
| [Ananas1Anass/RealTime-HairColorChange](https://github.com/Ananas1Anass/RealTime-HairColorChange) | U-Net 实时换色 pipeline |
| GitHub topic [`hair-segmentation`](https://github.com/topics/hair-segmentation) | 更多同类项目 |

### 3.3 如果要更高精度（对标施华蔻）

- **BiSeNet / face-parsing.PyTorch**（CelebAMask-HQ 训练，19 类含头发）—— 精度更高但更重
- 你仓库里的 `hair_seg_smp.onnx`（8.3 MB）就属这一档，适合服务端出高质量静态图

---

## 4. 可行性验证（已实测，非纸面推演）

用**你仓库现有的**模型和数据，在**你素材库里的真实照片**上跑通了：

```
分割:    151 ms  置信度 0.550  后端 smp_onnx
头发占比: 28.5%
重染色:   平均 18.6 ms/次（纯 CPU numpy，未做任何优化）
```

输出见 `/tmp/recolor_demo.jpg`：原图 + 蓝色9度 / 雾霾灰9度 / 树莓红7度 / 奶茶金9度，**发丝纹理、高光、层次全部保留**。

目标色**直接取自 `hairdye_palette_mapping.json`**，不是我瞎编的颜色。

### 实测暴露的问题（必须说清楚）

- **分割置信度只有 0.55**，边缘有溢色（背景天空和部分皮肤被染到）。原因是这里用的是 `hair_seg_smp.onnx` 回退后端，**不是** MediaPipe 的 `hair_segmenter.tflite`（本机没装 mediapipe）。
- 生产环境该用 MediaPipe 那个，精度和速度都更好。
- 边缘还需要 alpha matting 羽化处理才能达到施华蔻的观感。

---

## 5. 成本核算

### 5.1 经济成本

**当前生图方案**（实测数据）：

一次调用 `gemini-2.5-flash-image` 的真实 token 用量：
```json
{"completion_tokens": 1291, "completion_tokens_details": {"image_tokens": 1290}}
```

| 项 | 数值 |
|---|---|
| 输出 image tokens | 1290 /张 |
| Google 官方单价 | $30 / 1M output tokens |
| **单张成本** | **≈ $0.039 ≈ ¥0.28** |
| 每用户调用次数 | 1~2 次（standard 必发 + 有风险时加发 risk） |
| **每用户成本** | **¥0.28 ~ ¥0.56** |

> 4 张变体（低饱和/高饱和/冷调/暖调）是本地 HSV 微调生成的，**不额外花钱** —— 这个设计当初做得很对。
>
> ⚠️ 上表按 Google 官方价算。你实际走的是 openai-next 中转站，中转站有自己的加价/折扣，**真实单价要以他们账单为准**，我无法从 API 侧读到。转场视频走的是「按次计费站」，单价同理需要你查账单。

**实时方案**：

| 项 | 数值 |
|---|---|
| 模型调用 | **0 次** |
| 每次试色成本 | **¥0** |
| 边际成本 | **¥0**（用户试 100 个颜色也是 0） |

一次性成本：如果要商业 SDK 而非开源方案，Perfect Corp 的 Shopify 版从 **$379/月**起（100 SKU）；ModiFace 按年授权、单价需销售报价。**用 MediaPipe 开源方案则为 0。**

### 5.2 时间成本（用户等待）

| 方案 | 首次出结果 | 换一个颜色 |
|---|---|---|
| **当前生图** | **10.08 秒**（实测） | 再等 10 秒 |
| **实时渲染** | ~200ms（模型加载） | **50~70 ms/帧，连续实时** |

差距不是「快一点」，是**交互范式的改变**：从「提交→等待→看结果」变成「拖动滑块即时预览」。你判断「产品体验上肯定比生图好得多」是对的。

### 5.3 开发时间成本（估算，含假设）

假设：1 名前端 + 1 名算法，复用现有仓库资产。

| 阶段 | 工作量 | 说明 |
|---|---|---|
| Web 端 MediaPipe 接入 | 2~3 人日 | 官方 JS API，摄像头流 + 分割 |
| 亮度保留换色 shader | 2~3 人日 | 核心逻辑我已验证，需移植到 WebGL |
| 接入底色度数查表 | 1~2 人日 | 数据现成，接滑块 UI |
| 边缘羽化 / alpha matting | 3~5 人日 | **这块最容易超期**，决定「像不像施华蔻」 |
| 时序稳定（防抖动） | 2~3 人日 | 上帧 mask 反馈 |
| 移动端适配 + 性能调优 | 3~5 人日 | 低端机降分辨率策略 |
| **合计** | **13~21 人日** | 约 3~4 周（单人）/ 1.5~2 周（两人并行） |

如果只做**静态照片版**（不接摄像头实时流），砍掉时序和性能调优，约 **6~9 人日**。

---

## 6. 建议路径

**我的建议是分两步走，不要一次性替换：**

### 第一步：静态照片实时换色（低风险，1~2 周）
保留现有上传照片的流程，把「生图等 10 秒」换成「本地换色秒出 + 滑块调度数」。
- 复用仓库现有 `hair_segmenter.tflite` + `hairdye_palette_mapping.json`
- 成本直接归零，体验立刻提升
- 生图能力**保留**作为「高保真效果图」的付费/延迟档

### 第二步：摄像头实时预览（对标施华蔻，再 2~3 周）
在第一步验证过换色质量后，再接摄像头流和时序稳定。

### 生图方案不必完全废弃
实时渲染的天花板是「换颜色」，它做不到：
- 改变发型、发量、长度
- 模拟染后的质感变化（毛躁/光泽）
- 生成「染发过程」的转场视频

所以建议定位成：**实时渲染做试色探索（免费、无限次），生图做最终效果确认（1 次，高质量）**。这样既砍掉 90%+ 的调用成本，又保住生图的差异化价值。

---

## 7. 待确认

1. **中转站真实单价** —— 上面按 Google 官方价估算，你的实际成本要查 openai-next 账单
2. **目标平台** —— 微信小程序 / H5 / App？小程序的 AR 能力和 MediaPipe 的接入方式不同，会显著影响工作量估算
3. **是否接受 MediaPipe 的分割精度** —— 建议先用真实用户照片跑一批，对比施华蔻的效果再决定是否需要自训模型
