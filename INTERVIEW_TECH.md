# 染发 Agent · 全栈技术方案（面试用）

> 数字均为本项目实测。标注「未验证」的是设计思路，面试请照实说。

---

## 0. 一句话架构

> 「决策在云端，渲染在端侧。发色识别、风险判断、商品推荐走服务器；
> 头发分割和实时换色全跑在用户浏览器，服务器零参与。
> 这个分工是成本倒逼的——渲染走云端每次 ¥0.28 等 10 秒，放端侧无限次都是 0。」

```
端侧（浏览器）                      云端（2核2G / 3Mbps ECS）
├ MediaPipe 头发分割  8.3ms/帧      ├ 发色识别   gpt-4o-mini
├ WebGL 实时换色      <1ms          ├ 风险判断   规则引擎+知识库（查表，0 模型）
├ 底色明度测量        0.2s          ├ 语音问答   ASR + 意图分类 + RAG
└ 决策数据 8.5KB 静态 JSON          ├ 存档生图   gemini-2.5-flash-image
                                    └ 转场视频   wan2.2-kf2v-flash
```

**成本结构**：每用户 ≈ ¥0.29（识别 ¥0.01 + 生图 ¥0.28），试色部分 **¥0**。

---

## 1. 发色识别（多模态）

**方案**：`gpt-4o-mini` 双图输入（用户当前发照 + 博主目标发色图），
`response_format: json_object` 强制结构化输出。

**输出**：当前发色 / 目标色 / 度数 / 发长 / 发量 / 染发史 + 各项置信度。

**优化**：原本把手机原图 base64 直传，请求体好几 MB。
改为 Pillow 缩放到长边 1024 + JPEG q85 再编码。

| | base64 体积 |
|---|---|
| 原图直传（3600×4800） | 2.92 MB |
| 缩放后 | **0.10 MB（3.3%）** |

同时减少了 vision 模型的图像 tile 计费。`detail` 保持 `high` 未降级——发色识别的准确度比省 token 重要。

---

## 2. 底色度数识别（端侧，0 成本）

**关键认知**：染发行业的「度数」本质就是**明度**（1 度最深，10 度最浅）。
所以这是**物理量测量**，不需要模型。

```
① MediaPipe 分割拿 mask
② 取 mask 内像素的中位数 RGB   ← 中位数抗高光/阴影离群值，均值会被拉偏
③ RGB → Lab                    ← Lab 的 L 感知均匀，等间距 L 差 = 等间距视觉明度差
④ L 通道查阈值表 → 1~10 度
```

**实测到的硬边界**（面试可以主动讲）：

| 同一张照片，只改亮度 | Lab L | 识别度数 |
|---|---|---|
| 暗光房间 | 14.1 | **1 度** |
| 正常光照 | 27.1 | 3 度 |
| 窗边强光 | 42.8 | **4 度** |

**同一头发差 3 度**，而 3 度足以颠覆风险结论。
所以设计上是**算法预选 + 用户拿色卡校验**——让算法从「权威」降级为「助手」，反而更可信。

---

## 3. 三层风险判断（业务核心，纯查表）

**这是整个产品的价值核心，而且零模型成本。**

| | 判断什么 | 输入 | 数据源 |
|---|---|---|---|
| **第一层** | 能不能染 | 底色**度数** vs 该色所需度数 | `color_result_rules` 77 条 |
| **第二层** | 会不会偏色 | ①底色残留色相 + 染膏色<br>②当前发色 × 目标色中和矩阵 | `undertone` 表<br>`color_transition_rules` 156 条 |
| **第三层** | 出来多鲜艳 | 该度数下的真实呈色饱和度 | `color_effect_matrix` 77 条 |

**三层并列输出、互不否决**——「会偏色」不等于「不能染」。

> 面试价值：这是 PM 最该展示的技术 sense——**业务逻辑的正交性**。
> 我一开始把第二层塞进第一层（拿 biased 当否决权），结果试色页说「能染但偏色」、
> 方案页说「不能染」，代码怎么改都矛盾。根因是三个问题混在一个字段里。

**数据治理上做的两件事**：
- 官方矩阵只覆盖 5~9 度，3~4 度（中国用户绝大多数）整列为空，代码把「查不到」当成「不能染」→ 补录并标注来源区分官方/推断
- 矩阵存在讲不通的曲线（4度可染、5度不推荐、6度又可染）→ 加**单点凹陷平滑**，只在判定层修正、不改官方数据、可一键回退

---

## 4. 实时渲染（技术亮点）

### 不是 AI 生图，是每帧都跑的图形管线

```
摄像头帧 → ① MediaPipe 分割 → ② 亮度保留式换色 → ③ 合成输出
```

### ① 分割
Google MediaPipe HairSegmenter，763 KB，输出**置信度图**不是二值 mask（边缘才有过渡）。

| | 数值 |
|---|---|
| 官方 benchmark（Pixel 6 GPU） | 52 ms |
| 实测（M4 Mac 浏览器 720p 稳态） | **8.3 ms 中位 → 120 FPS** |
| 真机（Android） | 19 ms → 30 FPS |

### ② 亮度保留式换色 —— 逼真的关键

转到 **YCbCr** 后**只替换 Cb/Cr（色度），保留 Y（亮度）**。

> 因为发丝的纹理、高光、层次**全部编码在亮度里**。保住 Y，头发还是那束头发；
> 直接换 RGB 就变成「涂色块」，一眼假。

**漂亮的衍生结果**：布丁头不用特殊处理就自动正确——发根亮度低所以染不上，发尾漂过亮度高所以鲜艳。

### ③ 多档同屏几乎免费
最贵的分割是**共享**的，每个变体只是同一张 mask 配不同目标色再跑一次 shader（<1ms）。

| 4 档同屏 | ~10 ms → 100 FPS |
|---|---|

---

## 5. 语音问答（教程环节的 RAG）

**链路**（最长 4 次串行网络往返）：

```
录音 → ASR → 意图分类 → 问题改写 → KB 检索 → 答案润色 → TTS
       whisper  关键词快路径   LLM      向量+关键词   LLM
                失败才走 LLM
```

**检索是混合打分**，不是纯向量：

```python
score = 0.75 * semantic_score + 0.25 * lexical_score
#        ChromaDB 向量距离      关键词重叠
```

> 纯向量在垂直领域容易「语义相近但答非所问」，混合打分能拉回精确匹配。

**知识库**：42 条操作问答，embedding 用 `BAAI/bge-large-zh-v1.5`（中文效果优于通用模型）。

**做过的优化**：
- 两个 query（用户原话 + 改写后）原本各调一次 embedding API → 合并成 **1 次**（`embed_texts` 本就收 list）
- 改写的 LLM 调用原本无条件执行，但「问题不明确」分支根本用不到它 → 移入条件分支，**省一次往返**
- ASR 从本地 SenseVoice 改为云端同模型 → 砍掉 torch 全家桶约 **2 GB** 依赖和冷启动
- 只有本地 ASR 才需要 ffmpeg 转码，云端接口直收浏览器原始格式 → **去掉硬依赖 + 省一次子进程**

---

## 6. 商品推荐

`product_sku` 189 条 SKU / `products` 16 个商品 / `product_usage` 16 条用量策略。

**用量估算**：`发长 × 发量 × 商品的 quantity_policy_text` → 需要买几盒。
这就是为什么前面必须做发长发量识别——**它只服务于这一步，不参与风险判断**。

> 这个区分很重要：风险判断只需要底色度数 + 目标色，
> 所以「能不能染」可以在 0.2 秒内本地算出，vision 可以后台跑。

---

## 7. 生图与视频（重成本环节）

| | 模型 | 成本 | 耗时 |
|---|---|---|---|
| 存档效果图 | `gemini-2.5-flash-image` | 1290 image tokens ≈ **¥0.28** | 10.08 s |
| 染前染后转场视频 | `wan2.2-kf2v-flash`（首尾帧） | 按次计费 | 分钟级，轮询 |

**成本策略**：原本方案页自动生 5 张。改为——
**实时渲染做免费无限次探索，生图只在用户确认购买后生成 1 张存档**。
每用户从 ¥0.28~0.56 降到「只有高意向用户才产生 ¥0.28」。

---

## 8. 后端架构与性能

### 并发模型（最反直觉的一个修复）

**现象**：接手时「运行非常慢」，人越多越慢。

**根因**：23 个 FastAPI 端点全声明 `async def`，但函数体内全是同步阻塞调用
（SQLite、`urllib`、torch、ffmpeg），全项目**零处** `run_in_executor`。
asyncio 里 `async def` 内的同步阻塞会**独占 event loop**——
一个用户触发发色识别（超时上限 30 秒），**全服务器一起排队**。

**解法**：阻塞端点改回 `def`（FastAPI 自动丢线程池），重活用 `run_in_threadpool`。

```
5 并发：5.05s → 1.02s
期间无关请求响应：4948 ms → 2.6 ms
```

> 修法反直觉：不是加机器，是**去掉 async**。

### 网络层

- 8 处 `urllib` 改为共享 `httpx` 连接池（keepalive 60s）。上游是跨境中转，**每次重建连接就要重做 TLS 握手**

  ```
  真实 HTTPS 端点 6 次串行：8439 ms → 3101 ms（省 63%）
  ```
  > 注意：本地 mock server 压测会得出相反结论（无 TLS 可省，只剩 httpx 的 Python 开销）。收益完全来自跨境握手。

- **超时分级**：原本所有 LLM 调用共用 30s。意图分类只输出 10 个 token，没理由等 30 秒 → 拆成 8s / 15s / 30s 三档

### 容量测算（真正的瓶颈不在 AI）

| | @3 Mbps |
|---|---|
| MediaPipe 模型 763 KB | 2.1 s，每用户一次，之后浏览器缓存 |
| 教程视频（实测码率 1.3~2.8 Mbps） | **并发上限 1~2 人** |

> 「一个用户看 56 MB 教程视频，等于 15 个用户加载 MediaPipe。
> 所以优先级是视频转码 + CDN，不是升级机器。」

---

## 9. 技术选型的取舍

**为什么不用 AI 生图做实时？**
单次 10 秒、¥0.28，物理上不可能实时。且实时渲染改不了发型/发量/长度。
所以分工：**实时渲染做探索，生图做最终确认存档**。

**为什么不用商业 SDK？**
Perfect Corp 从 $379/月起，ModiFace 按年授权需报价。MediaPipe 是 Apache 2.0，成本 0。
代价是精度不如商业方案，边缘处理要自己补。

**端侧推理的额外好处**
摄像头画面**不出设备**——美妆/医美类产品可以直接写进隐私卖点。

---

## 10. 踩坑合集（挑 2~3 个讲最有效）

**① 偏色算错：加色模型 vs 减色模型** ⭐推荐讲
模拟「蓝染膏 + 黄底 = 偏绿」，结果算出灰蓝。根因是用了**线性 RGB 平均**（加色模型，蓝光+黄光=白），
而颜料混合是**减色过程**（蓝颜料+黄颜料=绿）。改乘法后色相 210° → 152°。
> 体现「能区分参数没调好 vs 模型选错了」。

**② 并发架构：症状是慢，根因是 async** ⭐推荐讲（见 §8）

**③ 三层判断揉成一层导致逻辑自相矛盾** ⭐推荐讲（见 §3）

**④ 漂浅三档渲染成一模一样**
只换目标色不够——摄像头里是同一束头发，亮度被保留机制锁死。
解法是引入 `lift`：漂浅的物理本质是**先把发丝提亮**，提亮后才吃得上色。
实测发区像素差 49.1 / 34.4，明显可区分。

**⑤ 漂浅档位全屏蒙白纱**
`lift` 直接作用于全图没乘 mask。修复一行：`lift * m`。
实测皮肤偏移 30/255、背景 43/255 → 均归零。

**⑥ 「要漂到 10 度」是硬编码**
`requiredLevel = max(8, 当前度数 + 4)`，一次都没查知识库。
6 度用户被告知要漂到 10 度，而知识库里蓝色 6 度起就能染。

---

## 11. 已知边界（主动交底，加分项）

- **边缘羽化未做**：分割边界有溢色，背景和皮肤会被染到。达到竞品观感需补 alpha matting，估 5~8 人日，最容易超期
- **时序防抖未做**：论文做法是把上一帧 mask 作为额外输入通道喂回网络
- **微信小程序不能直接用 MediaPipe**：要么用微信自己的 AR 能力、要么 WASM 移植，工作量翻倍
- **`getUserMedia` 只在 HTTPS/localhost 可用**：局域网 IP 访问拿不到摄像头
- **低端机降分辨率策略**（未验证）：实测 360p 下 7.6ms 比 720p 还快，思路是分割在 360p 跑、渲染在原分辨率跑，mask 放大插值

---

## 12. 表达建议

1. **先讲判断，再讲实现**。不要一上来讲 YCbCr，先讲「为什么不用生图」
2. **所有结论带数字**。「快很多」→「8.3ms / 120 FPS」；「省钱」→「¥0.28 → ¥0」
3. **主动讲坑**。①②③ 最能体现深度，因为症状和根因不在一个层面
4. **诚实标注边界**。技术交叉面最忌讳把没验证的说成已完成
