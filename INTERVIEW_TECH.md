# 染发 Agent · 技术方案（面试用）

> 所有性能与成本数字均为本项目实测，非估算。标注「未验证」的部分请照实说。

---

## 一、一句话讲清架构

> 「这个产品有两条技术路径。**决策链路在云端**——发色识别、风险判断、商品推荐；
> **渲染链路在端侧**——头发分割和实时换色全部跑在用户浏览器里，服务器零参与。
> 这个分工是成本倒逼出来的：渲染如果走云端，每次试色都要花钱；放到端侧，
> 用户试一百次颜色成本都是零。」

```
用户手机                                    服务器（2核2G / 3Mbps）
┌────────────────────────┐                ┌─────────────────────┐
│ 摄像头帧                │                │ 发色识别 vision      │
│   ↓ MediaPipe 分割 8.3ms│                │ 风险判断（查表）      │
│   ↓ WebGL 换色  <1ms   │                │ 商品推荐             │
│ 输出到屏幕              │                │ 存档生图（1次）       │
└────────────────────────┘                └─────────────────────┘
   服务器 CPU 占用 0                          只在关键节点介入
   边际成本 ¥0                                每用户约 ¥0.29
```

---

## 二、实时渲染怎么实现的（面试重点）

### 核心认知：这不是 AI 生图，是实时渲染

竞品（施华蔻染发魔镜、美图/抖音染发特效）看起来像 AI，实际是**每帧都跑的图形管线**：

```
摄像头帧 → ① 头发分割 → ② 亮度保留式换色 → ③ 边缘合成 → 输出
```

### ① 头发分割

用 Google MediaPipe 的 HairSegmenter（论文 *Real-time Hair Segmentation and Recoloring on Mobile GPUs*）。
输出的不是二值 mask，是**每个像素「是不是头发」的置信度**，这样边缘才有过渡。

| 指标 | 数值 |
|---|---|
| 模型体积 | 763 KB |
| 输入尺寸 | 512×512 |
| 官方 benchmark（Pixel 6 GPU） | 52 ms |
| 本项目实测（M4 Mac 浏览器，720p 稳态） | **中位 8.3 ms，p90 14 ms → 120 FPS** |
| 真机实测（Android） | 19 ms → 30 FPS |

### ② 亮度保留式换色 —— 「逼真」的关键

**这是整个方案最核心的一个技术判断，面试一定要讲清楚。**

把像素转到 **YCbCr** 空间后，**只替换 Cb/Cr（色度），保留 Y（亮度）**。

> 因为发丝的纹理、高光、阴影、层次感**全部编码在亮度里**。保住 Y，头发就还是那束头发，
> 只是换了颜色；如果直接替换 RGB，就变成了「涂色块」，一眼假。

```glsl
float Y  = dot(c, vec3(.299, .587, .114));   // 保留
float Cb = (c.b - Y) * .564;                 // 替换
float Cr = (c.r - Y) * .713;                 // 替换
```

**衍生的一个漂亮结果**：布丁头（发根黑、发尾漂过）不需要特殊处理就能自动正确——
发根亮度低 → 换色后仍然暗 → 呈现「染不上色」；发尾亮度高 → 色彩鲜艳。
再加一个 `receptivity = clamp((Y-0.10)/0.45, 0, 1)` 强化这个物理规律即可。

### ③ 多档位同屏几乎免费

一帧里最贵的是分割（8.3 ms）。而「标准/偏浅/偏深/偏色」这些变体，
本质是**同一张 mask 配不同目标色再跑几次 fragment shader**，每次 **< 1 ms**。

| 画面 | 每帧耗时 |
|---|---|
| 单色 | 8.3 ms → 120 FPS |
| 4 档同屏 | ~10 ms → 100 FPS |

> 面试话术：「贵的那一步是共享的，所以多档对比几乎不要钱。这让我们能做竞品做不到的
> 『理想效果 vs 翻车效果』并排对比。」

---

## 三、踩过的坑与解法（最能体现技术 sense）

### 坑 1：偏色算错了——加色模型 vs 减色模型

**现象**：模拟「蓝色染膏在黄色底上会偏绿」，结果算出来是灰蓝，怎么调都不绿。

**根因**：我用了**线性 RGB 平均**混合染膏色和底色残留。但 RGB 平均是**加色模型**（光的叠加），
而染膏和头发残留色是**颜料混合，属减色过程**。蓝光+黄光=白，但蓝颜料+黄颜料=绿。

**解法**：改用**乘法混合**（减色），乘完亮度会掉，再按原亮度拉回。

```
线性平均：rgb(34,131,223) → rgb(96,134,160)   色相 210°（还是蓝）✗
乘法混合：rgb(34,131,223) → rgb(53,144,103)   色相 152°（绿）✓
```

**延伸**：底色残留随漂浅程度变化（深棕→橙棕→橙→橙黄→黄→浅黄），
所以同一支蓝色染膏，8度黄底会偏绿，6度橙底则因为**蓝橙互补**而中和成灰浊。
这是色彩学的互补对（红↔绿、黄↔紫、蓝↔橙）在起作用。

> 面试价值：说明你能区分「参数没调好」和「模型选错了」——前者是调参，后者是认知错误。

### 坑 2：漂浅的三个档位渲染出来一模一样

**现象**：给「不漂 / 漂1次 / 漂2次」三档配了不同目标色，但画面上看不出区别。

**根因**：摄像头里**是同一束头发**。只换目标色，而亮度保留机制又把原始亮度锁死了，
三档自然长得一样。

**解法**：漂浅的物理本质是**把发丝提亮**，提亮之后才吃得上色。所以引入 `lift` 参数，
在着色器里先抬高亮度再上色。

```
实测发区平均像素差：不漂→漂1次 49.1，漂1次→漂2次 34.4，明显可区分
```

> 面试价值：体现「从物理机制反推实现」，而不是硬调视觉参数。

### 坑 3：漂浅档位下整个画面蒙了一层白纱

**现象**：用户反馈「看不清自己的脸」。

**根因**：`lift` 和去饱和系数**直接作用于全图，没有乘头发 mask**。

**解法**：`float lm = lift * m;` 一处修改。

```
修复前：皮肤偏移 30/255，背景偏移 43/255
修复后：均为 0（非头发像素完全原样透传）
```

> 面试价值：能定位到一行代码的根因，并用量化指标验证修复。

### 坑 4：并发架构导致「越用越慢」

**现象**：接手时后端「运行非常慢」。

**根因**：23 个 FastAPI 端点全部声明为 `async def`，但函数体内**全是同步阻塞调用**
（SQLite、`urllib`、torch 推理、ffmpeg 子进程），全项目**零处** `run_in_executor`。
在 asyncio 里，`async def` 内的同步阻塞会**独占整个 event loop**——
一个用户触发一次发色识别（超时上限 30 秒），**全服务器所有请求一起排队**。

**解法**：阻塞型端点改回 `def`（FastAPI 会自动丢线程池），重活用 `run_in_threadpool`。

```
5 并发压测：5.05s → 1.02s
期间无关请求的响应：4948 ms → 2.6 ms
```

> 面试价值：这是典型的「症状是慢，根因是架构」。而且反直觉——不是加机器，是**去掉 async**。

### 坑 5：判断逻辑自相矛盾

**现象**：试色页说「能染，但会偏色」，方案页却说「不能染」。

**根因**：把三个不同的问题揉成了一个判断。实际业务有**三层独立判断**：

| | 判断什么 | 输入 |
|---|---|---|
| 第一层 | 能不能染 | 底色**度数** vs 该色所需度数 |
| 第二层 | 会不会偏色 | 底色**残留色相** + 染膏色 |
| 第三层 | 出来多鲜艳 | 该度数下的真实呈色饱和度 |

我把第二层的「会偏色」当成了第一层的否决权。

**解法**：三层拆开、**并列输出、互不否决**。同时保证判定只在一处发生，其余页面只做展示。

> 面试价值：这是产品经理最该展示的技术 sense——**业务逻辑的正交性**。
> 三个问题混在一个字段里，代码怎么写都会矛盾。

---

## 四、成本与性能（量化能力）

### 端侧渲染 vs 云端生图

| | 云端生图 | 端侧实时渲染 |
|---|---|---|
| 单次成本 | 1290 image tokens ≈ **¥0.28** | **¥0** |
| 等待 | **10.08 秒** | **8.3 ms/帧，连续实时** |
| 换个颜色 | 再花 ¥0.28、再等 10 秒 | 免费、即时 |
| 服务器负载 | 每次都打后端 | **CPU 0、带宽 0** |

> 这不是「快一点」，是**交互范式的改变**：从「提交→等待→看结果」变成「拖滑块即时预览」。

### 首次加载成本（端侧方案的唯一代价）

| 资源 | 体积 |
|---|---|
| MediaPipe WASM 运行时 | 9.3 MB → **gzip 2.84 MB** |
| MediaPipe JS | 136 KB → gzip 39 KB |
| 模型 | 763 KB |

**优化**：WASM/JS 走 CDN，服务器只发 763 KB 模型并加长缓存；
在用户拍照、等识别结果的几秒内后台预加载 → **体感 0 等待**。

### 服务器容量测算（2核2G / 3 Mbps）

| | 数值 |
|---|---|
| 模型 763 KB @ 3 Mbps | 2.1 秒，每用户仅一次 |
| 教程视频（实测码率 1.3~2.8 Mbps） | **并发上限 1~2 人** ← 真正的瓶颈 |

> 面试话术：「我做过容量测算，发现瓶颈不在 AI，在视频带宽。一个用户看 56 MB 教程视频，
> 等于 15 个用户加载 MediaPipe。所以优先级是视频转码 + CDN，而不是升级机器。」

---

## 五、技术选型的取舍（会被追问）

### 为什么不用 AI 生图做实时？

生图单次 10 秒、¥0.28，**物理上不可能做到实时**。而且实时渲染的天花板是「换颜色」——
它改不了发型、发量、长度。所以定位是：
**实时渲染做试色探索（免费无限次），生图只在用户确认购买后生成 1 张高保真存档**。
这样砍掉 90%+ 调用成本，又保住生图的差异化价值。

### 为什么不用商业 SDK？

Perfect Corp（玩美移动）从 $379/月起，ModiFace 按年授权需报价。
MediaPipe 是 Apache 2.0 开源，模型已在项目里，成本 0。
代价是精度不如商业方案，需要自己补边缘处理。

### 端侧推理的额外好处

摄像头画面**不出设备**——这是隐私合规上的硬优势，在美妆/医美类产品里是可以写进卖点的。

---

## 六、可能被追问的问题 + 怎么答

**Q：识别准确度怎么保证？**
> 「底色度数本质是测明度：分割→取 mask 内像素**中位数** RGB（中位数抗高光/阴影离群值）
> →转 Lab（感知均匀）→L 通道查阈值表。
> 但我实测过一个硬边界：**同一张照片只改变亮度，识别结果从 1 度跳到 4 度**。
> 而 3 度误差足以颠覆结论。所以我的设计是**算法预选 + 用户用色卡校验**，
> 让算法从『权威』降级成『助手』——这反而更可信。」

**Q：还有什么没解决？**
> 「边缘羽化。现在分割边界会有溢色，背景和皮肤会被染到。
> 要达到竞品观感需要补 alpha matting，我估了 5~8 人日，是最容易超期的一块。
> 另外真机的时序稳定（防帧间抖动）也没做，MediaPipe 论文里的做法是把上一帧 mask
> 作为额外输入通道喂回网络。」

**Q：低端机跑不动怎么办？**
> 「实测 360p 下 7.6 ms，比 720p 还快。所以策略是按设备帧率动态降分辨率，
> 分割在 360p 跑、渲染在原分辨率跑——因为 mask 可以放大插值，视觉损失很小。」（未验证，是设计思路）

**Q：这个方案的边界在哪？**
> 「三个硬边界：① 只能换颜色，改不了发型发量；② 依赖摄像头权限，
> 且 `getUserMedia` 只在 HTTPS/localhost 下可用；③ 微信小程序不能直接用 MediaPipe，
> 要么用微信自己的 AR 能力、要么 WASM 移植，工作量翻倍。」

---

## 七、面试时的表达建议

1. **先讲判断，再讲实现**。不要一上来讲 YCbCr，先讲「为什么不用生图」。
2. **所有结论都带数字**。「快很多」→「8.3 ms，120 FPS」；「省钱」→「每用户 ¥0.28 降到 ¥0」。
3. **主动讲踩过的坑**。坑 1（加色/减色）和坑 4（async 阻塞）最能体现深度，
   因为它们都是「症状和根因不在一个层面」。
4. **诚实标注边界**。说清楚哪些实测过、哪些是设计思路、哪些没解决——
   技术交叉面最忌讳把没验证的说成已完成。
