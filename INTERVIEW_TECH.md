# 染发 Agent · 技术方案（产品经理视角）

> 目标：让你**真正理解**每个技术选择，而不是背名词。
> 所有性能与成本数字均为本项目实测；标注「未验证」的是设计思路。

---

## 0. 先建立一个全局认知

这个产品有两类计算，**贵贱差了无穷倍**：

| | 例子 | 单次成本 | 谁在算 |
|---|---|---|---|
| **贵的** | 发色识别、生图、生视频 | ¥0.01 ~ ¥0.28 | 云端大模型 |
| **便宜的** | 头发分割、换色、风险判断 | **¥0** | 用户手机 / 查表 |

**整个架构设计的主线就是：把能放进「便宜」那一栏的东西，尽可能都放进去。**

```
用户手机（浏览器里）                云端服务器
├ 头发分割      8.3 毫秒/帧        ├ 发色识别（多模态大模型）
├ 实时换色      不到 1 毫秒        ├ 语音问答（语音识别 + 检索 + 大模型）
├ 底色明度测量  0.2 秒             ├ 存档效果图（生图模型）
└ 风险判断      查表，0 毫秒        └ 染前染后转场视频（视频模型）
   ↑ 这些全部不花钱、不占服务器
```

结果：**用户在试色环节想试多少次都是 0 成本**，服务器 CPU 占用也是 0。

---

## 1. MediaPipe 是怎么把头发和脸分开的？

这是你最该讲透的一块，我从头讲。

### 1.1 它做的事叫「语义分割」

普通的图像识别是回答「这张图里有没有头发」（一张图一个答案）。
**语义分割**是回答「**这张图里的每一个像素，分别属于什么**」——
50 万个像素，就有 50 万个答案。

输出不是一张图，是一张**概率图**：

```
每个像素输出一个 0~1 的数
0.98 → 这里几乎肯定是头发
0.51 → 说不准（通常出现在发丝边缘）
0.02 → 几乎肯定不是头发
```

**为什么要概率而不是简单的「是/否」？**
因为头发边缘天然是半透明的——一根发丝可能只占半个像素，后面透出皮肤。
如果强行二选一，边缘就会像用剪刀剪出来一样生硬。有了概率，
我们就能在渲染时按 0.51 这种中间值做**半透明混合**，边缘才自然。

### 1.2 它凭什么知道哪里是头发

**靠大量人工标注的照片训练出来的。**

Google 找人把几万张人像照片里的头发区域**一个像素一个像素地涂出来**，
然后让神经网络反复看：「这张原图 → 对应这张涂好的答案」。
看得足够多之后，网络就归纳出了头发的视觉规律——

- 有细密的丝状纹理
- 长在头部轮廓的上方和周围
- 有特定的光泽反射方式
- 和皮肤的边界有特定的形态

**注意：它不是先找脸再排除脸。** 它是直接学会了「头发长什么样」。
所以它对光头、戴帽子、背景里有别人这些情况，判断依据都是同一套。

### 1.3 网络结构：先看懂，再画出来

用的是一种叫 **编码器-解码器**（业内也叫 U-Net 结构）的设计：

```
原图 512×512
   ↓ 编码器：不断缩小，同时提取越来越抽象的特征
   256×256 → 128×128 → 64×64 → 32×32
   （尺寸变小了，但「这是不是发丝纹理」这类判断变准了）
   ↓ 解码器：再放大回去，把判断落回到每个像素
   64×64 → 128×128 → 256×256 → 512×512
输出 512×512 的概率图
```

**为什么要先缩小再放大？**
缩小是为了「看懂整体」——只看一个像素永远分不清那是头发还是深色衣服，
必须看到周围一大片才知道「这是头顶」。
放大是为了「精确定位」——最终答案必须精确到每个像素。

**关键补充**：编码器每一层的信息会**直接抄一份给对应的解码器层**（业内叫跳跃连接）。
因为缩小过程中丢失的细节（发丝的精确位置）在放大时补不回来，
必须从原来的高分辨率层直接搬过来。**这是边缘能保持锐利的原因。**

### 1.4 视频不闪烁的秘密：把上一帧的答案喂回去

单张图片分割准，不代表视频看起来稳——
连续两帧只要判断略有差别，头发边缘就会**疯狂抖动**，非常廉价。

MediaPipe 的解法很聪明：**模型的输入不是 3 个通道（红绿蓝），是 4 个通道**。

```
第 4 个通道 = 上一帧算出来的头发遮罩
```

等于告诉模型：「上一帧头发在这个位置，你参考一下」。
模型因此倾向于给出**连续的、不跳变的**结果。

> 面试可以这么说：「它把时序一致性问题，转化成了一个额外的输入通道，
> 而不是在后处理里做平滑。这是很典型的『把问题挪到模型内部解决』的思路。」

### 1.5 为什么它能在手机上跑这么快

| | 数值 |
|---|---|
| 模型体积 | **763 KB**（一张照片的大小） |
| 官方 benchmark（Pixel 6 GPU） | 52 毫秒/帧 |
| 我实测（Mac 浏览器 720p） | **8.3 毫秒/帧 → 120 FPS** |
| 真机实测（Android） | 19 毫秒 → 30 FPS |

三个原因：
1. **模型是专门为手机设计的**——通道数少、用了移动端友好的卷积方式，牺牲一点精度换速度
2. **它跑在 GPU 上**——GPU 有几千个核心，图像处理天然可以「每个像素同时算」
3. **只做一件事**——只分头发，不做人脸关键点、不做背景分割，专用模型比通用模型小得多

---

## 2. 换色是怎么做到「像真的染了」的

### 2.1 先说为什么不能直接涂颜色

最直觉的做法是：找到头发区域，把这些像素全改成蓝色。
**结果一定是假的**——出来是一块死板的蓝色色块，没有发丝、没有光泽、没有层次。

因为**头发之所以看起来是头发，靠的是明暗变化**：
高光处亮、阴影处暗、发丝之间有细密的明暗交替。你一涂色，这些全没了。

### 2.2 关键洞察：颜色可以拆成「亮度」和「色相」两部分

我们平时说的 RGB（红绿蓝），是**混在一起的**——改任何一个值，
亮度和颜色会同时变。

但有另一种表示方式叫 **YCbCr**，它把颜色**拆开**了：

```
Y   = 亮度      （这个像素有多亮，跟是什么颜色无关）
Cb、Cr = 色度   （这个像素偏什么颜色，跟多亮无关）
```

**所以做法就很清楚了：只换色度，保留亮度。**

```
原来的发丝：亮度=各种明暗变化   色度=棕色
换色之后：  亮度=原封不动 ✓    色度=蓝色
```

结果：**它还是那束头发，纹理、高光、层次全在，只是颜色变了。**

> 这就是「亮度保留式换色」，也是所有专业换色方案（施华蔻、美图）的共同做法。

### 2.3 一个漂亮的意外收获：布丁头自动就对了

「布丁头」= 发根是黑的（新长出来的），发尾是漂过的浅色。

我们**什么特殊处理都没做**，它自动就正确了：

```
发根：原本亮度低（黑）→ 保留亮度 → 换色后仍然暗 → 呈现「染不上色」✓
发尾：原本亮度高（浅）→ 保留亮度 → 换色后鲜艳饱满 ✓
```

**这不是巧合，是真实的物理规律**——深色头发本来就吃不上色，
而亮度恰好就是「这段头发有多深」的度量。

我们只是又加了一行代码强化这个规律：亮度越高，上色越强。

### 2.4 多个效果同屏对比，几乎不要钱

一帧里最贵的是分割（8.3 毫秒）。而「标准 / 偏浅 / 偏深 / 偏色」这几个效果，
本质是**同一张头发遮罩，配不同的目标颜色再算一遍**——每次不到 1 毫秒。

```
1 个效果：8.3 毫秒  → 120 FPS
4 个同屏：约 10 毫秒 → 100 FPS
```

> **贵的那一步是共享的**，所以我们能做竞品不做的事：
> 「理想效果」和「翻车效果」并排给用户看。

---

## 3. 底色度数是怎么测出来的（不用 AI）

### 3.1 一个认知：度数就是明暗

染发行业说的「几度」，本质就是**头发有多亮**：

```
1 度 = 最深的黑    →    10 度 = 最浅的白金
```

所以这**不是识别问题，是测量问题**。就像量体温不需要 AI，测明度也不需要。

### 3.2 四步

```
① 用 MediaPipe 找出头发区域
② 取这些像素的「中位数」颜色
③ 换算成明度
④ 明度查表 → 得出 1~10 度
```

**第②步为什么用中位数不用平均值？**
头发上有高光（特别亮）和阴影（特别暗），这些是**极端值**。
平均值会被它们拉偏，中位数不会——排序后取中间那个，极端值影响不到。

**第③步为什么不直接用 RGB 的亮度？**
RGB 算出的亮度**不符合人眼感知**。有一种叫 Lab 的表示方式，
它的明度是**按人眼设计的**——明度差 10 和差 20，人看起来就是差两倍。
这正好对应发型师说的「差几度」。

### 3.3 实测到的硬边界（面试主动讲）

我拿同一张照片，只改亮度：

| 拍摄环境 | 识别结果 |
|---|---|
| 暗光房间 | **1 度** |
| 正常光照 | 3 度 |
| 窗边强光 | **4 度** |

**同一头发，差 3 度。** 而 3 度足以彻底改变结论（蓝色 6 度能染，5 度不能）。

**所以我的设计是：算法给初值，用户拿色卡校验。**
让算法从「权威」降级为「助手」——**承认误差反而让产品更可信**。

---

## 4. 风险判断：产品最值钱的部分，一个模型都没用

### 4.1 三个不同的问题

用户问「我能染吗」，其实里面藏着三个问题：

| | 问题 | 靠什么判断 |
|---|---|---|
| **第一层** | 能不能染？ | 你的底色够不够浅 |
| **第二层** | 会不会串色？ | 你头发里残留的颜色 + 染膏颜色 |
| **第三层** | 出来有多鲜艳？ | 同一支染膏在不同底色上显色不同 |

**这三层必须并列，不能互相否决。**「会偏色」≠「不能染」。

### 4.2 第二层最有意思：颜色是会打架的

头发漂浅的过程中，会依次呈现这些残留色：

```
黑 → 深棕 → 橙棕 → 橙 → 橙黄 → 黄 → 浅黄
（3度）              （6度）        （8度）
```

染膏盖上去，是和这个残留色**混合**，不是覆盖。所以：

```
蓝色染膏 + 8度的黄底 → 蓝+黄 = 绿      ← 偏绿
蓝色染膏 + 6度的橙底 → 蓝橙是互补色 = 灰浊  ← 变脏
```

**同一支染膏，不同底色，偏的方向完全不同。** 这就是为什么必须知道你几度。

### 4.3 实现方式：310 条结构化规则，查表

| 数据表 | 条数 | 内容 |
|---|---|---|
| 色系 × 底色度数 → 能不能染 + 原因 | 77 | 第一层 |
| 底色度数 → 残留什么颜色 | 7 | 第二层 |
| 当前发色 × 目标色 → 中和判断 | 156 | 第二层 |
| 色系 × 底色度数 → 真实呈色 | 77 | 第三层 |

> **面试价值**：这段最能体现判断力——
> 「产品最值钱的风险判断，我没有用 AI。因为这类知识是**有明确对错的行业规则**，
> 查表 100% 准确、0 成本、可解释、出错能追溯到具体哪条数据。
> 用大模型反而会有幻觉，还贵。**AI 要用在该用的地方。**」

### 4.4 踩过的坑：三个问题揉成一个字段

我一开始把「会偏色」当成了「不能染」的一种。
结果试色页说「能染但会偏色」，方案页说「不能染」——**代码怎么改都矛盾**。

根因是三个正交的问题混在了一个字段里。拆开之后问题自动消失。

> 「这是我学到最深的一课：**当一个 bug 怎么改都改不干净，通常是抽象层次错了，
> 不是代码写错了。**」

---

## 5. 语音问答：为什么不能只靠向量检索

### 5.1 完整链路

```
用户说话 → 语音转文字 → 判断意图 → 改写问题 → 查知识库 → 润色答案
                        （先用关键词    （口语转成
                         匹配，快且免费  适合检索的
                         ；命中不了才    短句）
                         调大模型）
```

**「先关键词、命中不了才调大模型」** 是个很实用的省钱设计——
用户说「下一步」这种高频指令，根本不需要惊动大模型。

### 5.2 检索：向量 + 关键词混合打分

**什么是向量检索**：把每句话变成一串数字（叫「向量」），
意思相近的句子，数字也相近。这样就能找到「意思像」的问答，
而不是只能找「字面一样」的。

**但纯向量在垂直领域会翻车**：
「染发要等多久」和「染发要洗多久」向量很接近，但答案完全不同。

**所以用混合打分**：

```
最终得分 = 0.75 × 语义相似度 + 0.25 × 关键词重合度
```

向量负责「理解意思」，关键词负责「拉回精确匹配」。

---

## 6. 生图与视频：最贵的部分，怎么控成本

| | 用的模型 | 单次成本 | 耗时 |
|---|---|---|---|
| 存档效果图 | `gemini-2.5-flash-image` | **¥0.28** | 10 秒 |
| 染前染后转场视频 | `wan2.2-kf2v-flash` | 按次计费 | 分钟级 |

**原本的设计**：进方案页就自动生 5 张效果图 → 每个用户 ¥0.28~0.56，且等 10 秒。

**改成**：
- **实时渲染做免费无限次探索**（用户想试多少颜色都行）
- **生图只在用户确认要买之后，生成 1 张高保真存档**

结果：只有**高意向用户**才产生那 ¥0.28。转化率高的用户才花钱，经济模型健康得多。

> 这个改动的本质是：**用免费的技术方案，去承接高频低价值的需求；
> 把付费的技术方案，留给低频高价值的时刻。**

---

## 7. 后端：一个反直觉的性能修复

### 7.1 现象

接手时后端「运行非常慢」，而且**人越多越慢**。

### 7.2 根因（这段值得完整讲）

Python 有一种叫「异步」的写法，本意是让服务器能同时处理很多请求。
它的原理是：**遇到等待时（比如等数据库、等网络），主动让出，先去处理别人的请求。**

但这有个前提：**你必须用异步的方式去等。**

原代码 23 个接口全都声明成了异步，**但里面全是同步的等待**——
读数据库、调 API、跑模型，都是「死等」。

结果就是：**一个用户触发发色识别（最长等 30 秒），整个服务器所有人一起卡住。**

> 打个比方：本来是「服务员点完单去后厨的路上可以先招呼下一桌」，
> 结果他站在后厨门口一动不动地等，整个餐厅都没人服务了。

### 7.3 修复方式很反直觉

**不是加机器，是把「异步」去掉**——改回普通写法，框架会自动把它们丢到线程池里跑。

```
5 个并发请求：5.05 秒 → 1.02 秒
期间一个无关请求的响应：4948 毫秒 → 2.6 毫秒
```

### 7.4 另外两个网络优化

**① 连接复用**
每次调用云端 API 都重新建立连接，而建立加密连接（握手）本身很耗时，
尤其上游是跨境服务器。改成复用同一条连接：

```
6 次连续请求：8439 毫秒 → 3101 毫秒（省 63%）
```

> 有个细节值得提：**用本地测试服务器压测会得出相反结论**——
> 因为本地没有加密握手可省，反而暴露了新库的额外开销。
> **收益完全来自跨境握手。能说清「什么情况下这个优化无效」比说它多好用更重要。**

**② 超时分级**
原本所有大模型调用共用 30 秒超时。但「判断用户说的是不是问题」
这种只输出 10 个字的调用，没理由等 30 秒。拆成 8 秒 / 15 秒 / 30 秒三档。

---

## 8. 容量测算：瓶颈根本不在 AI

服务器是 2 核 2G、带宽 3 Mbps。我算了一下各部分的带宽消耗：

| | 占用 |
|---|---|
| MediaPipe 模型 763 KB | 2.1 秒，**每个用户只下一次**，之后浏览器缓存 |
| 教程视频（实测每秒 1.3~2.8 Mbps） | **并发上限 1~2 人** ← 真正的瓶颈 |

> 「一个用户看一条 56 MB 的教程视频，等于 15 个用户加载 MediaPipe。
> 所以优先级是**视频转码 + 上 CDN**，不是升级服务器。」

**这句话在面试里很值钱**：它同时证明了三件事——会量化、懂全局、优先级判断有依据。

---

## 9. 为什么不用现成的商业方案

| 方案 | 情况 |
|---|---|
| Perfect Corp（玩美移动） | 从 $379/月起 |
| ModiFace（欧莱雅旗下） | 按年授权，需销售报价 |
| **MediaPipe** | **开源免费，模型 763 KB** |

代价是精度不如商业方案，边缘处理要自己补。

**另外一个容易被忽略的好处**：端侧推理意味着**摄像头画面不出设备**。
这在美妆/医美类产品里是可以直接写进隐私卖点的。

---

## 10. 还没解决的（主动交底 = 加分）

- **边缘羽化**：分割边界会溢色，背景和皮肤会被染到一点。要达到竞品观感需要补一种叫 alpha matting 的技术，估 5~8 人日，是最容易超期的一块
- **时序防抖**：MediaPipe 自带的上一帧反馈机制我还没在真机上调优
- **微信小程序跑不了 MediaPipe**：要么用微信自己的 AR 能力，要么做移植，工作量翻倍
- **摄像头权限限制**：浏览器规定只有 HTTPS 或本机才能调摄像头，局域网 IP 拿不到
- **低端机降分辨率**（未验证）：实测 360p 反而比 720p 快，思路是分割用低分辨率、渲染用原分辨率

---

## 11. 面试表达建议

**主讲三块**（各 2~3 分钟）：
1. **MediaPipe 怎么分割头发**（§1）——展示你真的懂技术原理
2. **亮度保留式换色**（§2）——展示你懂「为什么这样做效果才好」
3. **风险判断不用 AI**（§4）——展示判断力，这是 PM 最该有的

**四个原则**：
1. **先讲判断，再讲实现**。别一上来说 YCbCr，先说「为什么不用生图」
2. **所有结论带数字**。「快很多」→「8.3 毫秒、120 FPS」
3. **主动讲坑**。§4.4（抽象层次错了）和 §7（异步反而更慢）最能体现深度
4. **诚实标注边界**。技术交叉面最忌讳把没验证的说成已完成
