# 实时试色真实感修复

针对三个问题：**发色和目标色差太远**、**头发分割不准**、**覆盖效果失真**。

---

## 一、发色差太远 —— 根因在数据，不在渲染

### 核实过程

从 6 张种草参考图里采样真实染后发色，和 `color_effect_matrix` 的色值逐个比对。

采样方法：
- 先用 YCbCr 标准肤色域（`77≤Cb≤127, 133≤Cr≤173, Y>60`）排除面部，再排除高光和死黑
- 其中 **粉色失败**了 —— 粉发的 Cr 值落在肤色域内，被一起排除了
- **冷茶、冷棕** 也失败 —— 发区面积小，灰墙背景和衣服主导了样本
- 这三张改用逐张目视核对的发区框重采，取 20~80 分位的中位数

### 结果

| 色系 | 参考图实测 | 色卡值（矩阵） | 差距 |
|---|---|---|---|
| 蓝色 | H194 **S48** V57 | H207 **S96** V80 | 饱和度高 **2.0 倍** |
| 紫色 | H232 **S21** V42 | H293 **S56** V58 | 饱和度高 **2.7 倍**，色相偏 **61°** |
| 红色 | H347 **S47** V27 | H352 **S99** V73 | 饱和度高 **2.1 倍** |
| 粉色 | H5 **S31** V71 | H0 **S54** V92 | 饱和度高 **1.7 倍** |
| 黑茶色 | H17 S35 V43 | H15 S33 V24 | ✅ 准 |
| 奶茶灰棕 | H23 S43 V38 | H19 S35 V31 | ✅ 准 |

**根因**：这些行的 `source = 'sampled_from_chart'` —— 色值采自色卡/包装盒图。色卡是印刷或渲染的理想效果，饱和度天生拉满。真实染在亚洲黑发上，底色残留加发丝吸收，饱和度掉一半以上。

暗色系准，是因为它们本来就低饱和，色卡和真实差不多。**越鲜艳的颜色，色卡越不可信。**

紫色最严重：色卡是**洋红紫**（H293），真实是**冷灰蓝紫**（H232）。这已经不是深浅差异，是两个颜色。

### 标定规则

每个色系只有一个实测点（博主的结果），但矩阵每个色系有 4~5 个度数。所以：

1. **锚点** = 明度最接近实测值的那一档（明度是底色度数最强的信号）
2. **色相**：整族平移 `H_实测 − H_锚点`，偏移小于 4° 视为噪声不动
3. **饱和度**：整族乘 `S_实测 / S_锚点`，**且只许降不许升**
4. **明度**：完全不动

第 3 条的"只许降"和第 4 条是刻意的，因为有两张参考图有打光干扰：

- **奶茶灰棕** 算出来饱和度 ×1.80 —— 那张图字幕写着「\*室外阳光下」，阳光拉高了饱和和暖调
- **黑茶色** 算出来明度 ×1.83 —— 那是「榛果冷茶」，本身比黑茶浅，且室内强光

色卡的系统性偏差是**过饱和**；测出来说色卡不够饱和的，更可能是打光而非真实差异。而明度由底色度数决定，色卡的逐级递进是物理正确的，不该被博主的打光改写。

按这个规则，两个暗色族的校正系数自动收敛到 1.0（不变），和"暗色系本来就准"的观察一致。

### 数据改动

`color_effect_matrix` **新增 5 列**，色卡原值 `r/g/b/hex` **一列未动**：

```
r_real, g_real, b_real, hex_real, real_source
real_source = 'calibrated_from_reference_video_2026_08'
```

已写入 23 行。`_seed_color_knowledge_base` 的 upsert 只更新 `r/g/b/hex`，不会覆盖 `*_real`，重新 seed 安全。

老库靠 `_create_tables` 里的 `PRAGMA table_info` + `ALTER TABLE` 补列。

> **不动商品知识库**：`products` / `product_sku` / `product_price_snapshot` 完全没碰。
> `color_effect_matrix` 是效果矩阵，它的 rgb 列职责就是"染出来是什么颜色"。
> 也不影响「能不能染」的判定 —— 那看的是 `color_result_rules.result_quality`，与 rgb 无关。

### 接口改动

`GET /api/color-matrix` 的 `rgb` 优先返回实测值，缺失时回落色卡值：

```json
{ "rgb": [85,147,165], "hex": "#5593A5",
  "rgb_source": "calibrated_from_reference_video_2026_08",
  "rgb_chart": [11,108,165] }
```

`rgb_chart` 是色卡原值，商品页展示色号可以用它。**不要拿去渲染。**

前端无需改动即可吃到新值（`hair-mirror.tsx` / `layer3Vibrancy` / `biasedColor` 都读 `rgb`）。

### 校验

行数完整：`operation_qa 42` / `color_effect_matrix 65` / `products 16` / `product_sku 189` / `color_transition_rules 156`，全部未变。

---

## 二、覆盖效果失真 —— 两处 shader 缺陷

参考施华蔻魔镜的实拍截图，最关键的观察是：**它的发根始终保留原生黑色**，只有中段到发尾上色。这符合真实染发的物理事实（新生发未染到 + 发根色素最重），也是整个画面真实感的最大来源。

### ① 明度被目标色污染 → 纹理感消失

```glsl
// 旧
float nY=mix(Y,Y*.72+tY*.28,a*.65);
```

展开后 `nY = Y*0.818 + tY*0.182` —— **发丝明暗对比被压到 81.8%**。而纹理感全部编码在明度对比里，这一行直接对应"看起来很假、没有纹理"。

```glsl
// 新：Y 原样输出
vec3 o=vec3(Y+1.403*nCr, Y-.714*nCr-.344*nCb, Y+1.773*nCb);
```

染发本身不会提亮头发，提亮只能靠漂 —— 那是 `lift` 的职责，不该混进色度替换里。

### ② `recept` 下限太高 → 发根整片变色

```glsl
float a=m*mix(.30,1.,recept)*strength;   // 旧：下限 0.30
float a=m*mix(.05,1.,recept)*strength;   // 新：下限 0.05
```

下限 0.30 意味着最暗的发根也吃 30% 的色。施华蔻的下限接近 0。

---

## 三、分割不准 —— 两处修复

### ① 置信度图被当硬 mask 用

```js
b[i] = f[i] * 255;   // 旧：线性映射
```

MediaPipe 给的是**置信度图**不是二值图。线性当 alpha 用，置信度 0.3 的背景像素也会吃 30% 的色 —— 这就是"背景上的东西被误当成头发"的直接来源。

改为在 shader 里做软阈值（免费，不占 CPU）：

```glsl
float m=smoothstep(.45,.75,texture2D(mask,uv).r);
```

### ② 没有时域平滑 → mask 逐帧抖动

MediaPipe HairSegmenter 模型内部本来有「上一帧 mask 作为第 4 输入通道」的时序机制，但 **Web 版没暴露这个接口**，逐帧独立推理会让发际线闪烁。

在 JS 侧补一阶 EMA：

```js
const MASK_EMA = 0.55;   // 上一帧占比，越大越稳但越迟滞
v = prev[i] * MASK_EMA + f[i] * (1 - MASK_EMA);
```

尺寸变化（切换摄像头/分辨率）时重置。

---

## 四、还没做的

| | 说明 |
|---|---|
| **空间梯度** | 目前 `recept` 按**明度**衰减发根，而真实的发根—发尾梯度是**空间性**的（新生发 vs 旧发）。要做得像施华蔻，需要人脸关键点定位发际线，按到发际线的距离额外衰减 |
| **发丝级羽化** | mask 边缘目前只有纹理 LINEAR 插值，碎发处仍有色块边界。需要 alpha matting |
| **粉色参考不足** | 粉色只有 8/9 两档有色值，且实测点来自一位浅金底色博主。深底色的粉色表现没有实测支撑 |
| **真机验证** | 本次改动只做了接口层和数值验证。摄像头在开发容器里不可用，**shader 的观感必须在真机上确认** |
