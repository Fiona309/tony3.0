# 用户流程与设计决策

> 2026-08-05｜本文记录实时试色接入后的完整流程，以及背后的取舍理由。

## 产品定位

解决居家染发的翻车风险：**AI 判断风险 + 推荐最适合的商品**。
实时试色不是新功能，是把「能不能染」这个判断**从文字变成用户自己脸上的样子**——
具象化的风险认知比"百分之多少能染"更能催化下单。

---

## 一、核心洞察：等待与试色的矛盾不存在

原本担心的是：识别 12 秒 + 生图 10 秒之后才让用户看到效果，太长。

**但风险判断根本不依赖 vision API。** 代码验证（`color_rule_kb.evaluate_profile`）只吃三个输入：

| 输入 | 来源 | 耗时 |
|---|---|---|
| `current_level` 底色度数 | 本地分割 + 中位数取色 + Lab 亮度查表 | **0.2 秒，不联网** |
| `target_name` 目标色 | 用户从视频种草进来时已知 | 0 |
| `dye_history` 染没染过黑发 | 原本来自 vision → **改为直接问用户是非题** | 0 |

`hair_length` / `hair_volume` **不参与风险判断**，只在 `product_kb._units_needed()` 里算"买几盒"。

所以：**「能不能染」+「染出来什么样」都能在 0.2 秒内给出，vision API 只服务后面的商品推荐，可以后台跑。**

关键不是减少等待，是**把等待藏在用户主动想做的事情后面**——用户在试色时是愉悦、专注的，这 12 秒是负成本的。

---

## 二、流程

```
① 视频种草                         目标色已知
      ↓ 「试试染同款」
② 拍一张照
      ↓
③ 底色确认                          模型加载 1~2s，天然转场
   · 色卡比对，算法预选              vision API 此刻发起，后台跑
   · 一个是非题：染过黑色吗？
      ↓
④ 实时试色 ＋ 风险结论  ← 主战场      零等待
      ↓ 「看完整方案」
⑤ 完整方案 ＋ 商品推荐               vision 数据已就绪，用户无感
      ↓ 「就染这个」
⑥ 生成 1 张标准存档图 → 教程
```

**③ 是整个设计的枢纽**，同时解决三件事：给算法误差兜底、建立"被认真分析"的仪式感、给 vision 争取后台时间。

---

## 三、底色度数怎么来的，以及它的边界

染发行业的「度数」本质就是明度：1 度=最深的黑，10 度=最浅的白金。所以这是**物理量测量**，不需要模型。

```
① MediaPipe 分割 → 每像素"是不是头发"的置信度
② 只取 mask>0.6 的像素，求 RGB 中位数
③ RGB → Lab（sRGB 线性化 → XYZ(D65) → Lab）
④ L 通道查阈值表 [15,25,35,45,55,65,75,85,95,100] → 1~10 度
```

- **用中位数不用均值**：头发有高光和阴影，都是离群值，均值会被拉偏。
- **用 Lab 不用 RGB**：Lab 的 L 是感知均匀的，等间距 L 差 = 等间距视觉明度差，正好对应发型师说的"差几度"。

### ⚠️ 准确度边界（实测）

同一张照片只改变亮度：

| 场景 | Lab L | 识别度数 |
|---|---|---|
| 暗光房间 | 14.1 | **1 度** |
| 正常光照 | 27.1 | 3 度 |
| 窗边强光 | 42.8 | **4 度** |

**同一头发，识别结果差 3 度。** 而蓝色在 5 度是 `not_recommended`、7 度是 `normal`——3 度误差会完全颠覆结论。

**所以手动修正是必需环节，不是兜底选项。** 且不该做成"AI 说你是 5 度，不对就改"（等于让用户质疑你），应做成**发型师的实际做法：拿色卡比对**，算法预选一个让用户确认。算法从"权威"降级为"助手"，反而更可信。

---

## 四、与施华蔻的三个关键差异

差异来自定位差异，不是为了不一样而不一样。

| | 施华蔻 | 我们 | 为什么 |
|---|---|---|---|
| 底部分组 | 按色系（热卖/冷棕/蓝灰/亚麻） | **按能不能染** | 它是品牌货架要卖全色卡；我们是风险顾问。用户不必理解"冷棕色系"，只需看懂"我现在能不能染" |
| 效果展示 | 只显示理想效果 | **可一键看翻车效果** | 翻车预警是立身之本。施华蔻不敢吓跑买家，我们敢——这恰恰是信任来源 |
| 滑块含义 | 漂几次 | **用量/停留时间**（需漂时才切成漂浅档） | 居家染发翻车的真实原因，也是教程的价值锚点 |

### 风险分组的实际效果

| 底色 | 现在就能染 | 需漂1次 | 需漂2次 | 不建议 |
|---|---|---|---|---|
| **3 度**（天生黑发） | **0** | 1 | 9 | 3 |
| 5 度 | 1 | 9 | 3 | 0 |
| 7 度 | 10 | 3 | 0 | 0 |
| 9 度 | **13** | 0 | 0 | 0 |

中国用户多为 3~4 度天生黑发，**最常见的情况是"想染的几乎都要先漂"**。
看着像转化灾难，但这正是产品存在的理由——用户不知道这件事，翻车就是这么来的。诚实说出来才是价值。

---

## 五、生图的定位

留**一张标准效果图作为存档**，触发时机是**用户确定要染之后**，不是方案页。

- 方案页免费无限试 → 只有转化率高的用户才触发生图
- ¥0.28 只花在有购买意向的用户身上，经济模型远比"人人生 5 张"健康
- 用途：存档 + 染发时对照 + 分享种草

---

## 六、成本

| 环节 | 模型 | 成本 |
|---|---|---|
| 底色识别 | 本地 MediaPipe | **¥0** |
| 风险判断 | 静态 JSON 查表 | **¥0** |
| 实时试色（无限次） | 本地 WebGL | **¥0** |
| 属性识别（发长等） | `gpt-4o-mini` | ~¥0.01/用户 |
| 存档图 | `gemini-2.5-flash-image` | ¥0.28，仅高意向用户 |

服务器（2核2G / 3Mbps）增量负担：**每个新用户 763KB 模型，一次，之后浏览器缓存**。
WASM 走 CDN，试色阶段服务器 CPU 与带宽占用均为 0。

---

## 七、待办

- [ ] ③ 底色确认屏尚未实现（目前直接进 ④，底色可在顶部改）
- [ ] 接入主流程（现为独立路由 `/mirror`）
- [ ] 「染过黑色吗」是非题
- [ ] 分组标签在窄屏会折行，需调整
- [ ] 边缘羽化 / alpha matting（决定"像不像施华蔻"的观感）
- [ ] 视频素材 224MB 在 3Mbps 上 1~2 人并发即满，上线前必须转码 + CDN
