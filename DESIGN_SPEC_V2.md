# 交互设计图生成说明 · 拍照后 → 商品推荐前

给图像模型用的逐屏布局说明。画布 **375 × 812 pt（iPhone 竖屏）**，
所有百分比均为该高度的占比。

覆盖 5 个主屏 + 4 个浮层。商品推荐页不在本文档范围内。

---

## 全局风格

### 基调

手账 / 贴纸风。奶油底 + 墨线描边 + 糖果粉主色。圆角偏大、边框略带手绘不规则感。
不要拟物、不要玻璃拟态、不要深色科技风。

### 色板（必须严格使用）

| 用途 | 色值 |
|---|---|
| 主背景 | `#FDFAF5` 奶白 |
| 次背景 / 卡片内嵌 | `#F9F4ED` |
| 卡片底 | `#FFFFFF` |
| 主文字 | `#3D3733` 墨黑 |
| 次文字 | `#6B635C` |
| 弱文字 / 标签 | `#A79E96` |
| 主 CTA / 强调 | `#EE8AAE` 糖果粉 |
| 强调深色（数字、激活标签） | `#E2789F` |
| 分隔线 / 描边 | `#EBE3D9` |
| 通过 / 能染 | 边框 `#8BC79C`　底 `#EEF7F0`　图标 `#3F8A56` |
| 警示 / 风险 | 边框 `#E8C47A`　底 `#FFF8E4`　文字 `#7A5A12` |
| 否定 / 不能染 | 图标 `#B4514A` |
| 试色屏深色底 | 页面 `#111014`　头尾栏 `#141317`　滑块条 `#1B1A1F` |

### 圆角

卡片 `20px`　内嵌块 `14–16px`　按钮 `999px` 胶囊　色块 `10px`

### 字号

| 层级 | 字号 | 字重 |
|---|---|---|
| 主结论标题 | 22pt | 900 |
| 区块小标题 | 13pt | 900 |
| 清单行的值 | 15pt | 900 |
| 正文 / 说明 | 12pt | 400–700 |
| 标签 / 注释 | 10–11pt | 400 |
| 主按钮 | 15pt | 900 |

### 装饰规则（重要）

- 贴纸、爱心、星星、马克笔下划线：**只用在标题区和正向内容上**
- **风险区块必须干净** —— 不放任何贴纸装饰，只用素净边框。
  「断发风险」旁边配粉色爱心会让语气打架，可信度是这里唯一要保护的东西

---

## 通用组件

### 顶栏（所有非试色屏）

```
高度 ≈ 96pt
┌──────────────────────────────────────┐
│  (×)          屏幕标题            空位 │  ← 44pt 行，标题 13pt/900 居中
│                                      │
│ ▬▬▬▬▬  ▬▬▬▬▬  ▬▬▬▬▬  ▬▬▬▬▬          │  ← 4 段进度条，各占 1/4 宽
│ 认识头发  能不能染   看效果   怎么买    │     10pt 标签
└──────────────────────────────────────┘
```

**进度条规则**：当前段的横杠为 `#EE8AAE`、标签 `#E2789F`/900；
已完成段横杠 `rgba(61,55,51,.35)`；未到达段横杠 `rgba(61,55,51,.12)`、标签 `#A79E96`。
**不出现任何「第 N 步 / 共 4 步」数字。**

### 底部 CTA 区

```
高度 ≈ 76pt，上方 1px 分隔线 #EBE3D9
[      粉色胶囊主按钮，左右各留 20pt 边距      ]
```

---

# 屏 1 · 确认头发

**用途**：确认识别对不对。整屏是一张四行清单，不出现任何判断结论。

```
┌──────────────────────────────────────┐  0
│  (×)     先确认一下你的头发             │
│  ●━━━  ────  ────  ────              │  ← 第 1 段激活
│  认识头发 能不能染  看效果  怎么买        │
├──────────────────────────────────────┤  12%
│  你想染的  ( ● 蓝色 )                  │  ← 胶囊标签，白底墨线描边
│                                      │     圆点用目标色实心
├──────────────────────────────────────┤  17%
│  ┌────────────────────────────────┐  │
│  │                                │  │
│  │        用户上传的照片            │  │  ← 高 190pt，圆角 20
│  │      （裁切顶部，露出发根）       │  │     占比 23%
│  │                                │  │
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤  42%
│  ┌────────────────────────────────┐  │
│  │ 发色      棕色              ⌄   │  │
│  ├────────────────────────────────┤  │  ← 白底卡片，行高 52pt
│  │ 度数      6 度         (?)  ⌄   │  │     行间 1px 线 #EBE3D9
│  ├────────────────────────────────┤  │
│  │ 发长      齐肩发            ⌄   │  │
│  ├────────────────────────────────┤  │
│  │ 漂染历史   从未漂过          ⌄   │  │
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤  68%
│    识别受光线影响，点任意一行可以修改      │  ← 11pt 居中 #A79E96
│                                      │
│         （留白，不要填满）              │
├──────────────────────────────────────┤  90%
│  [   确认，看能不能染蓝色   ]           │
└──────────────────────────────────────┘  100%
```

### 清单行的构成

| 元素 | 规格 |
|---|---|
| 标签（发色/度数…） | 左对齐，固定宽 68pt，13pt，`#A79E96` |
| 值 | 紧随其后，15pt/900，`#3D3733` |
| `(?)` 圆形按钮 | **仅「度数」行有**。直径 18pt，圆形描边 `rgba(61,55,51,.25)`，底 `#FDFAF5`，内含小问号 |
| `⌄` 下拉箭头 | 最右，14pt，`#A79E96` |

> **`(?)` 只出现在度数行**：这是四项里误差最大的一项，需要辅助；
> 但色卡默认收起，不能让这一屏从「清单」变成「色卡页」。

### 变体 1-X：矛盾提示态

当漂染历史与度数对不上时，把底部那行灰色说明**替换**为一块警示卡：

```
┌────────────────────────────────────┐
│ 你选了「漂过 2 次」，但照片看起来只有   │  ← 底 #FFF8E4，边框 #E8C47A
│ 5 度，两个对不上，再确认一下？         │     圆角 14，文字 12pt #8A6413
└────────────────────────────────────┘
```

**不阻塞**：主按钮仍然可用，不置灰。

---

## 浮层 1-A · 色卡（点 `(?)` 出现）

从底部升起，遮罩 `rgba(0,0,0,.35)`。

```
        （上方 24% 是被遮罩的原页面）
├══════════════════════════════════════┤  高度 76%
│  怎么看自己的头发是几度            (×) │  ← 14pt/900
│                                      │
│  在自然光下把这张色卡靠近发根比对——     │  ← 12pt #6B635C 行高 1.7
│  发根是没被染过的原生发色，最能代表你    │
│  的底色。发尾如果染过或漂过，会比发根    │
│  浅，不要拿发尾比。                    │
│                                      │
│  ┌──┬──┬──┬──┬──┐                  │
│  │1度│2度│3度│4度│5度│                │  ← 1~10 度真实发丝照片色卡
│  ├──┼──┼──┼──┼──┤                  │     两行五列，圆角 16
│  │6度│7度│8度│9度│10│                │
│  └──┴──┴──┴──┴──┘                  │
└══════════════════════════════════════┘
```

## 浮层 1-B · 下拉选择器（点任意行出现）

同样从底部升起。标题如「选择你的漂染历史」，下方竖排选项卡片：

```
│  选择你的漂染历史                 (×) │
│  ┌────────────────────────────────┐  │
│  │ 从未漂过                        │  │  ← 选中态：边框 #EE8AAE
│  └────────────────────────────────┘  │     底 rgba(238,138,174,.1)
│  ┌────────────────────────────────┐  │     文字 #E2789F
│  │ 漂过 1 次                       │  │
│  └────────────────────────────────┘  │  ← 未选中：边框 #EBE3D9 白底
│  ┌────────────────────────────────┐  │
│  │ 漂过 2 次                       │  │
│  ├────────────────────────────────┤  │
│  │ 漂过 3 次及以上                  │  │
│  ├────────────────────────────────┤  │
│  │ 染过黑发                        │  │
│  └────────────────────────────────┘  │
```

**漂染历史固定 5 项，就是上面这五个，不多不少。**

---

# 屏 2A · 能染

**用途**：给结论 + 说清风险 + 展示掉色过程。**绝对不放效果图**。

```
┌──────────────────────────────────────┐  0
│  (←)      你的染发方案                 │
│  ────  ●━━━  ────  ────              │  ← 第 2 段激活
├──────────────────────────────────────┤  12%
│  ✓ 你可以染蓝色                        │  ← 22pt/900，✓ 图标 #3F8A56
│    你的 6 度底色可以直接上色，不用先漂。   │  ← 12pt #6B635C
├──────────────────────────────────────┤  22%
│  你要注意的                            │  ← 13pt/900
│  ┌────────────────────────────────┐  │
│  │ 颜色会和博主有差距 —— 你的 6 度底  │  │  ← 底 #FFF8E4 边框 #E8C47A
│  │ 色还残留橙，颜色盖上去会被带偏      │  │     标题 12.5pt/700 #7A5A12
│  │ → 试色屏拖到最右边可以看到偏色的    │  │     动作行 11.5pt #8A6B28
│  │   样子，先看清楚再决定             │  │     圆角 16
│  └────────────────────────────────┘  │
│  ┌────────────────────────────────┐  │
│  │ 发根和发尾会不一样 —— 你漂过 1 次， │  │
│  │ 发尾吃色快、发根慢                │  │
│  │ → 先涂发尾停 10 分钟，再涂发根一起  │  │
│  │   冲，两段颜色才接得上             │  │
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤  56%
│  掉色过程概览                          │  ← 13pt/900
│  蓝色大概能保持 2-3 周 （行业参考值）     │  ← 12pt，周数加粗，
│                                      │     「（行业参考值）」10.5pt #A79E96
│  ▓▓▓  ▓▓▓  ▓▓▓  ▓▓▓  ▓▓▓            │  ← 5 等分色块，高 44pt
│  第1周 第2周 第3周 第4周 第5周           │     圆角 10，边框 rgba(61,55,51,.2)
│  蓝色  浅蓝色 蓝绿色  绿色  黄绿色        │     周数 10pt #A79E96
├──────────────────────────────────────┤     名称 10.5pt/700
│         （留白）                       │
├──────────────────────────────────────┤  90%
│  [    看看染在我头上  →    ]           │
└──────────────────────────────────────┘
```

### 掉色色带的真实色值（6 度底色染蓝色）

**这五个色必须严格照用，它们是算出来的物理结果，不是配色选择：**

| 周 | 名称 | 色值 |
|---|---|---|
| 第 1 周 | 蓝色 | `#354645` |
| 第 2 周 | 浅蓝色 | `#45463A` |
| 第 3 周 | 蓝绿色 | `#614C30` |
| 第 4 周 | 绿色 | `#8B582B` |
| 第 5 周 | 黄绿色 | `#C46E28` |

> 从冷调蓝灰一路推到暖橙 —— 这就是「蓝色掉着掉着变绿再变黄」的样子。

### 这一屏刻意没有的东西（不要画进去）

- ❌ 色度尺（那是屏 2B 的）
- ❌ 接近度百分比 / 进度环
- ❌ 任何效果图、前后对比图
- ❌ 过敏测试、凡士林等操作提示

---

# 屏 2B · 不能染

**用途**：讲清差多少 + 给两条出路。

```
┌──────────────────────────────────────┐  0
│  (←)      你的染发方案                 │
│  ────  ●━━━  ────  ────              │
├──────────────────────────────────────┤  12%
│  ✕ 现在还不适合染蓝色                   │  ← 22pt/900，✕ 图标 #B4514A
│    不是永远不能染，是当前底色还不够浅。    │  ← 12pt #6B635C（关键：化解焦虑）
├──────────────────────────────────────┤  22%
│  ┌────────────────────────────────┐  │
│  │ ▪▪ ▪▪ ▪▪ ▪▪ ▓▓ ▒▒ ▪▪ ▪▪ ▪▪      │  │  ← 色度尺：9 格等宽，高 36pt
│  │  1  2  3  4  5  6  7  8  9      │  │     圆角 6，格间距 3pt
│  │                                │  │
│  │ 你现在 5 度        蓝色要 6 度    │  │  ← 11pt/700，右侧 #E2789F
│  │ ┌────────────────────────────┐ │  │
│  │ │        还差 1 度            │ │  │  ← 底 #FDFAF5，13pt/900
│  │ └────────────────────────────┘ │  │     数字 #E2789F
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤  40%
│  为什么现在不建议直接染                  │  ← 13pt/900
│  · 底色太深，蓝色盖不上去，容易发黑发脏    │  ← 12pt #6B635C 行高 1.7
│  · 居家漂发有断发和刺激头皮的风险，       │     **此区块不放任何贴纸装饰**
│    漂到位不容易                        │
├──────────────────────────────────────┤  55%
│  ┌────────────────────────────────┐  │
│  │ 现在就能直接染的                  │  │  ← 白底卡片
│  │                                │  │
│  │   ●        ●         ●         │  │  ← 圆形色球 56pt
│  │  红色     冷茶色     冷棕色       │  │     名称 11.5pt/700
│  │ 保色4-6周 保色6-8周  保色6-8周    │  │     保色期 10pt #A79E96
│  └────────────────────────────────┘  │
├──────────────────────────────────────┤  78%
│         （留白）                       │
├──────────────────────────────────────┤  90%
│  [  仍想要蓝色，看漂浅效果  →  ]        │
└──────────────────────────────────────┘
```

### 色度尺的九格底色（从 1 度到 9 度）

```
#1C1613  #2B211B  #3F2E23  #573C29  #7A5334  #A3763F  #C39A55  #DCBB7D  #ECD9AE
```

**两个标记框**：
- **当前度数（5）**：2pt 实线边框 `#3D3733`，格下数字 900 字重
- **门槛度数（6）**：2pt 实线边框 `#EE8AAE`，格下数字 900 字重
- 其余格无边框，数字 9.5pt `#A79E96`

### 替代色球的真实色值（6 度底色）

| 名称 | 色值 | 保色期 |
|---|---|---|
| 红色 | `#7E4347` | 4-6 周 |
| 冷茶色 | `#2F2625` | 6-8 周 |
| 冷棕色 | `#2C1F22` | 6-8 周 |

> 这些是**真实染后色**，不是色卡上那种鲜艳色 —— 必须偏暗偏灰，
> 用鲜艳色会让用户对效果产生错误预期。

### 版面分区规则

**「换个色」是回头，「看漂浅效果」是前进 —— 两者必须在版面上分开：**
换色的三个色球留在「现在就能直接染的」那张卡片里，
底部只留唯一一个前进 CTA。不要做成一对并排的同级按钮。

---

# 屏 3 · 实时试色（能染模式）

**用途**：看见。画面是全部价值，文字一律不叠在画面上。

整屏深色底 `#111014`。

```
┌──────────────────────────────────────┐  0
│ (←)  ● 蓝色 · 能直接染 (i)    6度 ✎  │  ← 头栏 #141317
│  ────  ────  ●━━━  ────              │     状态点：能染=#7FD39A
│  认识头发 能不能染  看效果  怎么买        │     标题 13pt/900 白
├──────────────────────────────────────┤  13%
│                                      │
│                                      │
│                                      │
│         实时摄像头画面                 │  ← 占 60%，全出血
│      （头发已被换成目标色，            │     右上角有一枚半透明黑色
│        发根保留原生深色，              │     圆形按钮 ⇆「按住看染前」
│        高光保持白色不被染）             │
│                                      │
│                                      │
├──────────────────────────────────────┤  73%
│        你的 6 度底色的官方呈色          │  ← 12pt/700 居中白
│  ●━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │  ← 滑块条 #1B1A1F 区域
│  偏浅      和目标色一样       可能偏色    │     激活档 900 白，其余 rgba(255,255,255,.45)
├──────────────────────────────────────┤  85%
│ [ 换个颜色 ]  [    就要这个 →     ]   │  ← 左：描边胶囊，白色 25% 边框
└──────────────────────────────────────┘     右：粉色实心胶囊，flex-1
```

### 滑块轴（能染模式）

固定四档，**最右端永远是「可能偏色」**：

```
偏浅 —— 和目标色一样 —— 偏深 —— 可能偏色
```

滑到「可能偏色」时：滑块 accent 变 `#E08A84`，该档标签也变 `#E08A84`，
上方说明文字变成「橙底残留会把颜色带偏」。

> 偏色不是可选开关，是**一定要看到**的一档，所以在同一根轴上，
> 不要画成 toggle 开关。

---

# 屏 3' · 实时试色（漂浅模式）

与屏 3 **同一个屏幕**，只有四处不同。用户从屏 2B 进来时是这个形态。

```
┌──────────────────────────────────────┐
│ (←)  ● 蓝色 · 需要先漂浅 (i)   3度 ✎ │  ← 状态点改 #E5C169（黄）
│  ────  ────  ●━━━  ────              │
├──────────────────────────────────────┤
│                                      │
│         实时摄像头画面                 │  ← 60%
│    （头发被提亮到漂后的金黄色，          │
│      再叠上目标色）                    │
│                                      │
├──────────────────────────────────────┤
│      漂完约 7 度，够染了                │  ← 达标时文字变 #7FD39A
│  ━━━━━━━━━━━┋━━━━━━━━━━━━●          │  ← ┋ 是黄色门槛竖线 #E5C169
│  不漂       漂浅1次       漂浅2次       │     高 20pt，宽 2pt
├──────────────────────────────────────┤
│ [ 换个颜色 ]  [    就要这个 →     ]   │
└──────────────────────────────────────┘
```

### 四处差异一览

| | 能染模式 | 漂浅模式 |
|---|---|---|
| 顶栏状态点 | 绿 `#7FD39A`「能直接染」 | 黄 `#E5C169`「需要先漂浅」 |
| 滑块档位 | 偏浅 / 一样 / 偏深 / 偏色 | 不漂 / 漂浅1次 / 漂浅2次 |
| 滑块形态 | 平铺无标记 | **多一根黄色门槛竖线** |
| 档位说明 | 「你的 6 度底色的官方呈色」 | 「漂完约 5 度，还差 1 度」→ 达标后「漂完约 7 度，够染了」 |

**门槛线是这一屏最关键的一笔** —— 过了这条线才染得上。
进屏时滑块默认落在门槛档（即第一个够染的位置）。

---

## 浮层 3-A · 换色面板

从画面区底部升起，**上方仍露出约 36% 的实时画面**（这是关键：换色时画面不消失）。

```
│                                      │
│      实时画面（仍在跑，跟着色卡变）       │  ← 露出 36%
│                                      │
├══════════════════════════════════════┤  面板高 64%（相对画面区）
│  换个颜色              [ 用这个色 ]    │  ← 标题 15pt/900 白
│  按你的 6 度底色 ·                     │     按钮：粉色实心胶囊
│  点一下上面的画面立刻变                  │  ← 11pt rgba(255,255,255,.5)
│                                      │
│  现在能直接染                          │  ← 11.5pt/900 #7FD39A
│  ┌────────────────────────────────┐  │
│  │ ●  蓝色                     ✓  │  │  ← 选中：2pt 边框 #EE8AAE
│  │    保色 2-3 周                  │  │     底 rgba(255,255,255,.07)
│  └────────────────────────────────┘  │     色球 44pt 圆形
│  ┌────────────────────────────────┐  │     名称 14pt/900 白
│  │ ●  红色                        │  │     副行 11pt rgba(255,255,255,.55)
│  │    保色 4-6 周                  │  │
│  └────────────────────────────────┘  │  ← 未选中：边框 rgba(255,255,255,.1)
│  ┌────────────────────────────────┐  │
│  │ ●  紫色                        │  │
│  │    保色 1-2 周                  │  │
│  └────────────────────────────────┘  │
│                                      │
│  要先漂才能染                          │  ← 11.5pt/900 #E5C169
│  ┌────────────────────────────────┐  │
│  │ ○  粉色                        │  │  ← 色球透明度 45%
│  │    还差 2 度，要先漂浅            │  │
│  └────────────────────────────────┘  │
└══════════════════════════════════════┘
```

### 面板的三条规则

1. **点色卡后面板不关闭**，上方画面立刻变色，选中标记移动 —— 用户可以一个一个点过去边点边看
2. **引导写在色卡本身上**（色球 + 保色期 / 差几度），不要额外的信息条
3. **分两组**，中间用组标题分隔；不能染的组色球降透明度但仍可点

### 色球真实色值（6 度底色）

| 颜色 | 色值 | 分组 |
|---|---|---|
| 蓝色 | `#354645` | 现在能直接染 |
| 红色 | `#7E4347` | 现在能直接染 |
| 紫色 | `#513651` | 现在能直接染 |
| 冷茶色 | `#2F2625` | 现在能直接染 |
| 冷棕色 | `#2C1F22` | 现在能直接染 |
| 粉色 | `#EBAAA3` | 要先漂才能染（透明度 45%）|

---

## 浮层 3-B · 判断详情（点顶栏 `(i)` 出现）

浅色浮层压在深色试色屏之上，内容与屏 2A 一致。

```
│      （上方 20% 为深色画面 + 遮罩）      │
├══════════════════════════════════════┤  高度 80%
│  ✓ 蓝色 · 能直接染                (×) │  ← 底 #FDFAF5，15pt/900
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 颜色会和博主有差距 —— …          │  │  ← 与屏 2A 同款警示卡
│  │ → 试色屏拖到最右边…              │  │
│  └────────────────────────────────┘  │
│                                      │
│  大概能保持 2-3 周 （行业参考值）        │  ← 12pt/700
│  ▓▓  ▓▓  ▓▓  ▓▓  ▓▓                 │  ← 同款掉色色带，色块高 36pt
│  第1周 第2周 第3周 第4周 第5周           │
│  蓝色  浅蓝色 蓝绿色  绿色  黄绿色        │
└══════════════════════════════════════┘
```

---

# 生成时的六条硬性要求

1. **屏 2A / 2B 里不能出现任何效果图或人脸照片。** 判断屏讲道理，试色屏才看效果
2. **屏 3 / 3' 上不能有超过三行的文字叠在画面上。** 结论只在顶栏一行
3. **不出现「第 N 步 / 共 4 步」**，进度一律用四个阶段名
4. **不出现接近度百分比、成功率、匹配度**这类数字指标
5. **所有色块必须用文档里给的真实色值**，不要自行挑好看的配色 —— 这些是算出来的物理结果
6. **风险区块保持素净**，不加贴纸、爱心、星星；装饰只用在标题区和正向内容上

---

# 屏幕清单（共 9 张）

| # | 屏幕 | 说明 |
|---|---|---|
| 1 | 屏 1 确认头发 | 常态 |
| 2 | 屏 1 矛盾提示态 | 底部换成黄色警示卡 |
| 3 | 浮层 1-A 色卡 | 从底部升起 |
| 4 | 浮层 1-B 漂染历史选择器 | 5 个选项 |
| 5 | 屏 2A 能染 | 风险 2 条 + 掉色色带 |
| 6 | 屏 2B 不能染 | 色度尺 + 替代色 |
| 7 | 屏 3 试色（能染模式） | 四档滑块 |
| 8 | 屏 3' 试色（漂浅模式） | 三档滑块 + 门槛线 |
| 9 | 浮层 3-A 换色面板 | 上方仍露出画面 |
