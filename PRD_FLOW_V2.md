# 种草 → 试色 → 商品：流程改版 PRD（初稿）

改版范围：**拍照之后、商品推荐之前**。种草视频页、Landing、拍照页、商品页均不动。

---

## 0. 这版要解决的问题

旧流程从拍照到看见效果有 7 道关卡，且每一屏都在同时回答好几个问题，用户找不到重点。
本版把它压成 **4 屏**，并给每一屏定一个唯一职责：

| 屏 | 唯一职责 | 明确不放什么 |
|---|---|---|
| 屏1 确认头发 | 确认我看对了 | 任何判断结论 |
| 屏2A 能染 | 能染，以及要注意什么 | 效果图 |
| 屏2B 不能染 | 差多少、有哪两条路 | 效果图 |
| 屏3 实时试色 | 看见 | 三行以上的文字 |

一条自检标准：**屏2 出现效果图，或屏3 出现长文，就是越界了。**

---

## 1. 链路

```
                  拍照
                    │
              ┌─────▼─────┐
              │ 屏1 确认头发 │
              └─────┬─────┘
                    │
        ┌───────────┴───────────┐
      能染                  不能染 / 染过黑发
        │                       │
   ┌────▼────┐            ┌─────▼────┐
   │ 屏2A     │◄──── 换色 ─┤ 屏2B     │
   └────┬────┘            └─────┬────┘
        │                       │ 坚持要这个色
        └───────────┬───────────┘
              ┌─────▼─────┐
              │  屏3 试色   │ ←─┐
              └─────┬─────┘   │ 换色面板（浮层，不跳屏）
                    │         └───┘
              ┌─────▼─────┐
              │  商品推荐   │
              └───────────┘
```

**换色在两个地方语义不同**，实现也不同：

- 屏2B 的换色 = **横向重新判断**，留在屏2，刷新成新色的结论
- 屏3 的换色 = **浮层探索**，画面不离开，点一个色上面立刻变

---

## 2. 屏1 · 确认头发

### 内容

```
进度：认识头发 ─ 能不能染 ─ 看效果 ─ 怎么买
你想染的  ●蓝色                      ← 目标色角标
[ 你的照片 ]
─────────────────────
发色       棕色            ⌄
度数       5 度      (?)   ⌄        ← 问号点开才出色卡
发长       齐肩发          ⌄
漂染历史    从未漂过        ⌄
─────────────────────
[ 确认，看能不能染蓝色 ]
```

### 三个设计决定

**① 顶部挂目标色。** 用户是带着「我要染蓝色」进来的。这一屏若一个字不提蓝色，
"确认我的头发"就成了没有理由的填表，动机链是断的。CTA 也因此写成
「确认，看能不能染**蓝色**」而不是「看分析结果」。

**② 色卡收在问号后面，不默认展开。** 度数是四项里误差最大的
（实测同一张照片只改亮度，识别结果能从 1 度跳到 4 度），需要辅助；
但默认铺开会把这一屏从"清单"变成"色卡页"，喧宾夺主。

**③ 漂染历史 × 度数交叉校验。** 度数识别本身不可靠，单看没有容错。
加上漂染历史后，它从"唯一依据"降级成"两个依据之一"：

| 漂染历史 | 合理度数区间 | 依据 |
|---|---|---|
| 从未漂过 | 1–6 | 亚洲人天生发色上限约 6 度 |
| 漂过 1 次 | 4–8 | 一次漂浅约 +2 度 |
| 漂过 2 次 | 6–10 | |
| 漂过 3 次及以上 | 7–10 | |
| 染过黑发 | 1–4 | 黑色素沉积，底色必深 |

矛盾时当场提示，不阻塞提交：
> 你选了「漂过 2 次」，但照片看起来只有 5 度，两个对不上，再确认一下？

### 漂染历史枚举

后端 `EDITABLE_OPTIONS.dye_history` 改为 5 项：
`natural / bleached_1 / bleached_2 / bleached_3_plus / dyed_black`

旧值 `dyed_no_bleach` / `bleached_1_2` / `unknown` 由 `normalize_dye_history()`
在出口统一归一化，历史存档不会把原始枚举名显示给用户。

> **为什么必须把 1 次和 2 次拆开**：一次漂浅约 +2 度，这正是
> "够不够染这个色"的判定粒度。旧的 `bleached_1_2` 把两档合并，
> 等于抹平 4 度的差异，判断会失真。

---

## 3. 屏2A · 能染

```
✓ 你可以染红色
  你的 5 度底色可以直接上色，不用先漂。

你要注意的                              ← 风险在掉色之前
 ⚠ 颜色会和博主有差距 —— 你的底色偏深，颜色出来会比博主闷一些
   → 试色屏拖到最右边可以看到偏色的样子，先看清楚再决定
 ⚠ 发根和发尾会不一样 —— 你漂过 1 次，发尾吃色快、发根慢
   → 先涂发尾停 10 分钟，再涂发根一起冲，两段颜色才接得上

掉色过程概览
红色大概能保持 4-6 周（行业参考值）        ← 保色期在色带之上
 ▓▓   ▓▓   ▓▓   ▓▓   ▓▓
第1周 第2周 第3周 第4周 第5周
红色  玫红  橘红  橘色  浅橘

[ 看看染在我头上 → ]
```

### 为什么不放色度尺

尺子回答的是"**差多少**"，那是屏2B 用户的障碍。对能染的用户，
"你富余 2 度"没有任何行动意义。为了和 B 版对称而加是错的 —— **对称本身不是价值**。
踩线过门槛的风险由风险清单一句话表达即可。

### 为什么不放接近度百分比

- 88% 没有参照系，用户无法解读是好是坏
- 接近度这件事，屏3 的实时画面表达得好得多 —— 她自己看，自己判断像不像
- 用数字替代视觉是错的

### 为什么不放效果图

屏2 放了效果图，「看看染在我头上」这个 CTA 就没有动机了；
而且静态图和实时渲染必然对不上，用户会问哪个是真的。
**屏2 讲道理，屏3 看效果。**

---

## 4. 风险清单

### 收录标准：每条风险必须配得出一个具体动作

说不出该做什么的提示没有指导意义。「染发会损伤发质」是典型 —— 所有人都知道，
且给不出可执行的下一步，**故删除**。

### 分成两类，放在两个地方

**判断类**（影响"我要不要染"）→ 屏2A

| 风险 | 触发条件 | 动作 |
|---|---|---|
| 颜色会和博主有差距 | 偏色标注 / 踩线 / 饱和度显著低于最佳 | 试色屏拖到最右看偏色 |
| 发根发尾不一样 | 漂染历史 ≠ 从未漂过 | 先涂发尾停 10 分钟再涂发根 |

> **偏色与显色淡合并成一条**：两者同因（底色没处理干净 / 不够浅），
> 对用户是同一件事 ——「染出来跟博主不一样」。拆成两条是拿技术分类去切用户感受。

**操作类**（决定要染之后才相关）→ 商品页之后 / 染发教程，**不放屏2A**

| 风险 | 动作 |
|---|---|
| 过敏 | 染前 48 小时耳后测试 |
| 皮肤染色 | 发际线涂凡士林 |

### 「染过黑发」不是风险，是判定条件

黑色染膏色素沉积极深，漂都漂不动 —— 这是所有染过黑发的人共有的**客观事实**。
它属于「不能染」，直接进屏2B，不在屏2A 里当警告出现。
（后端 `color_rule_kb` 早已把 `dyed_black` 路由到 `_salon_required_decision`。）

---

## 5. 掉色过程

### 数据从哪来

| 需要什么 | 来源 | 是否新增 |
|---|---|---|
| 每周的**色值** | 算出来的，见下 | ❌ 零新数据 |
| 每周的**中文阶段名** | 新表 `color_fade` | ✅ 30 行 |
| **保色期** | 新表 `color_fade` | ✅ |

**色值不需要素材，也不需要人工标定**，因为掉色的物理过程就是
"染膏色素流失、底色残留逐渐暴露"，两个端点数据库里都有：

```
起点（第1周）= color_effect_matrix 的官方呈色
终点（第5周）= RESIDUAL_UNDERTONE 的残留底色
中间        = 两者的减色混合（复用 biasedColor）
```

合成分两段，缺一不可：

1. **减色混合**负责*色相怎么变* —— 蓝色配黄底会经过绿，这是掉色最有观感的一段
2. **向残留底色收敛**负责*最后退成什么* —— 权重取 `t²`，前期慢后期快，与色素流失曲线一致

只做 ① 末周会停在一个被 desat 压扁的浑浊色，五格看上去几乎一样；
只做 ② 就是一条直线渐变，中间那段绿会消失。

实测输出（蓝色 @ 7 度）：
```
蓝色[85,147,165] → 浅蓝[108,143,130] → 蓝绿[139,143,100] → 绿[174,142,73] → 黄绿[220,150,50]
```

> **副作用是对的**：掉色色带与实时试色的「可能偏色」档共用同一个函数，
> 两处结果物理上必然一致，不会自相矛盾。

### color_fade 表

```sql
CREATE TABLE color_fade (
  color_zh TEXT, week INTEGER CHECK (week BETWEEN 1 AND 5),
  stage_name TEXT, hold_weeks_min INTEGER, hold_weeks_max INTEGER,
  source TEXT, updated_at TEXT, PRIMARY KEY (color_zh, week)
);
```

| 色系 | 保色期 | 掉色路径 |
|---|---|---|
| 蓝色 | 2-3 周 | 蓝 → 浅蓝 → 蓝绿 → 绿 → 黄绿 |
| 紫色 | 1-2 周 | 紫 → 浅紫 → 灰紫 → 米黄 → 黄 |
| 粉色 | 1-2 周 | 粉 → 浅粉 → 藕粉 → 米粉 → 浅黄 |
| 红色 | 4-6 周 | 红 → 玫红 → 橘红 → 橘 → 浅橘 |
| 黑茶色 | 6-8 周 | 黑茶 → 深茶 → 茶棕 → 浅茶棕 → 浅棕 |
| 奶茶灰棕 | 6-8 周 | 奶茶灰棕 → 浅灰棕 → 浅棕 → 米棕 → 浅黄棕 |

**保色期与阶段名是行业通识参考值，不是实测**，`source = industry_reference`，
UI 必须显示「行业参考值」字样。后续靠用户回访数据替换为实测值。

---

## 6. 屏2B · 不能染

```
✕ 现在还不适合染蓝色
  不是永远不能染，是当前底色还不够浅。

┌ 色度尺 ────────────────────┐
│ 1 2 3 4 [5] (6) 7 8 9      │  [5]=你  (6)=门槛
│ 你现在 5 度      蓝色要 6 度  │
│        还差 1 度            │
└───────────────────────────┘

为什么现在不建议直接染
 · 底色太深，蓝色盖不上去，容易发黑发脏
 · 居家漂发有断发和刺激头皮的风险，漂到位不容易

┌ 现在就能直接染的 ──────────┐
│  ●红色    ●冷茶色   ●冷棕色  │  ← 点了回屏2A（横向重判）
│ 保色4-6周 保色6-8周 保色6-8周 │
└───────────────────────────┘

[ 仍想要蓝色，看漂浅效果 → ]     ← 唯一的前进
```

**回头与前进在版面上分开**：换色属于「现在就能直接染的」那个区块，
底部只留一个前进 CTA。旧版把两者做成一对同级按钮，一个是回头一个是前进，层级混淆。

**门槛度数取自知识库** `minDyeableLevel()`，不是写死的 `max(8, 当前+4)`。
实测各色最低可染度数：蓝 6 / 红 3 / 紫 6 / 粉 8 / 黑茶 3 / 奶茶灰棕 3。

**替代色一律用真实呈色**（`rgb`），不用色卡色（`rgb_chart`）。
色卡是印刷/渲染的理想效果，鲜艳色系饱和度普遍是真实染后色的两倍，
拿它当色球会让用户产生错误预期。

---

## 7. 屏3 · 实时试色

```
← │ ●蓝色 · 能直接染  ⓘ │ 5度 ✎     ← 结论在顶部标题栏
   认识头发 ─ 能不能染 ─[看效果]─ 怎么买

        实时摄像头画面                  ← 占满剩余空间

  和目标色一样                          ← 随档位变的说明
  偏浅 ─── 一样 ─── 偏深 ─── 偏色
[ 换个颜色 ]        [ 就要这个 → ]
```

### 结论放顶部，不放底部窄条

顶部是用户找"我在看什么"的地方，且不和滑块抢注意力。
底部的窄信息条会被当成装饰，实测没人看。点 ⓘ 从底部推上完整判断
（内容与屏2A 一致，同一个组件，避免两处各说各话）。

### 偏色进滑块，不做开关

偏色不是可选项，是**一定要看到**的一档，所以固定在同一根轴的最右端。
做成开关的话默认关 = 用户永远不会打开 = 风险提示等于没有。

### 能染 / 不能染共用这一屏，靠三处区分

| | 能染 | 不能染 |
|---|---|---|
| 标题栏 | ●绿点 · 能直接染 | ●黄点 · 需要先漂浅 |
| 滑块语义 | 偏浅 / 一样 / 偏深 / 偏色 | 不漂 / 漂1次 / 漂2次 |
| 滑块形态 | 平铺 | **多一根黄色门槛线** |
| 档位说明 | 「和目标色一样」 | 「漂完约 7 度，够染了」/「还差 1 度」 |

**门槛线是关键设计**：过了这条线才染得上。用户拖动时自己发现"得漂两次"，
比任何文案说教都有效 —— 结论是她自己得出来的。

进屏时滑块自动落在门槛档，让她一进来就看到"漂到这里才行"的那个结果。

---

## 8. 换色面板

**问题**：独立换色屏会产生 `试色 → 换色屏 → 判断屏 → 试色` 的三跳往返，换三次就晕。
**但取消换色屏、把色卡塞进试色屏也不行** —— 引导会被压成一根窄条，谁也不看，而且挤。

**解法：换色面板做成半屏浮层，点色即时变，面板不关。**

```
        实时画面（跟着色卡变）        ← 上方 36% 仍然实时
═══════════════════════════
 换个颜色              [用这个色]
 按你的 5 度底色 · 点一下上面的画面立刻变

 ── 现在能直接染 ──
 ┌───────────────────────┐
 │ ● 红色      保色 4-6 周 │ ✓
 └───────────────────────┘
 ┌───────────────────────┐
 │ ● 冷茶色    保色 6-8 周 │
 └───────────────────────┘

 ── 要先漂才能染 ──
 ┌───────────────────────┐
 │ ○ 蓝色   还差 1 度，要先漂浅 │
 └───────────────────────┘
```

### 三个关键

**① 点色卡 → 上面画面立刻变，面板不关。**
用户可以一个一个点过去、边点边看，满意了再「用这个色」收起。
**从头到尾没有离开过画面**，这就消灭了往返。

**② 引导住在色卡本身上。** 每张卡自带色块 + 保色期 + 一句判断。
这就是引导 —— 不需要额外的信息条，也不会被忽略，因为它就是用户正在读的东西。

**③ 分两组，中间有明确分隔。** 「现在能直接染」/「要先漂才能染」，
不能染的色球降透明度但仍可点（点了滑块自动切成漂色模式）。

### 郑重讲一次，轻量探索无限次

| | 屏2A（第一次） | 换色面板（探索时） |
|---|---|---|
| 何时 | 从种草视频进来，只有一个目标色 | 用户主动想换 |
| 讲多少 | 完整：风险 + 掉色 5 周 | 一句：保色期 + 判断 |
| 想看完整的 | — | 选定后点顶部 ⓘ |

---

## 9. 阶段进度条

**不写「第 3 步 / 共 4 步」。** 这不是线性表单，而是有分支、可回头的决策流程，
数字进度会产生两处故障：

1. 换色重新判断时编号原地不动，看起来像卡住了
2. 走到 4/4 以为结束了，商品页却又冒出来 —— 这是流失最高的一处

改成四个阶段名：`认识头发 ─ 能不能染 ─ 看效果 ─ 怎么买`。
「怎么买」始终摆在那里，用户知道后面还有一段；换色时停在「能不能染」不动
也是正确的表达，因为她确实还在这个阶段。

---

## 10. 前后端一致性

| 用户动作 | 前端 | 必须同步回后端 |
|---|---|---|
| 屏1 确认四项 | 本地 state | `PATCH profile`（原有） |
| 试色屏改度数 | `changeBaseLevel` | `current_hair.color.level` |
| 屏2B / 面板换色 | `changeTargetColor` | `target_color` |

**换色必须写回 `target_color`**，否则方案页与商品推荐仍按原来那个色算，
用户会拿到对不上的商品。为此 `/api/mock/videos` 新增返回 `target_color`
（完整 HairColor，含 level/rgb/lab），前端不能只靠色名去猜。

**三层判断在四屏共用同一组函数、同一个底色**
（`layer1CanDye` / `layer2BiasRisk` / `layer3Vibrancy`），
所以屏2、屏3、方案页的结论物理上不可能不一致。

**前端不持有任何数据副本**：色值、判定、掉色路径、保色期全部来自
`GET /api/color-matrix`，没有写死的 mock。

---

## 11. 本版新增/改动的后端接口

| 接口 | 改动 |
|---|---|
| `GET /api/color-matrix` | 新增 `fade` 字段（色系 → 保色期 + 5 周阶段名） |
| `GET /api/mock/videos` | 每个视频新增 `target_color` |
| `EDITABLE_OPTIONS.dye_history` | 6 项 → 5 项，拆开漂 1 次 / 2 次 |
| 新表 `color_fade` | 6 色系 × 5 周 = 30 行，`source=industry_reference` |

---

## 12. 待办

- [ ] 保色期与阶段名目前是行业参考值，需要用户回访数据替换为实测
- [ ] 操作类风险（过敏测试、凡士林）落到商品页之后的教程环节
- [ ] 屏1 仍需等 vision 识别约 12 秒，「零等待」优化未做
- [ ] 发量目前仍在屏1 的数据模型里但不展示，按计划应挪到商品页询问
